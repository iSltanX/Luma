# خريطة المهام

مؤشرات فقط: «أريد أن أغيّر س ← هذه الملفات». **لا قاعدة واحدة هنا** — القواعد في `Luma.md` و`IMPLEMENTATION.md`، وهذا الملف يشير إليها.

اليوم الذي يُقرأ فيه هذا الملف بدل الوثيقتين هو اليوم الذي فشل فيه.

## أين أبدأ

| أريد أن… | الملفات | القاعدة الحاكمة |
|---|---|---|
| أغيّر نافذة التطبيق أو مقاسها | `src-tauri/tauri.conf.json` | `IMPLEMENTATION.md` §٩ |
| أغيّر قائمة macOS | `src-tauri/src/lib.rs` → `build_menu` | §١ «ما يجب أن يأتي من المنصة» |
| أمسّ مسار التخزين | `src-tauri/src/lib.rs` → `luma_data_dir` | §٤ — **يحرسه اختبار** |
| أضيف أمرًا بين النواة والواجهة | `src-tauri/src/lib.rs` → `generate_handler!` | §٢ حدود الطبقات |
| أغيّر خطًا أو أضيف وزنًا | `public/fonts/` + `src/lib/fonts.css` | §٨ — Almarai أربعة أوزان فقط |
| أغيّر لونًا أو مقاسًا | `src/tokens/source.json` ثم `node src/tokens/generate.mjs` | §٩ — **لا لون يدوي** |
| أضيف مكوّنًا | `src/components/` — قابله في الصفحة ١٧ أولًا | §١٢ قاعدة الاستيراد |
| أضيف أيقونة | تُصدَّر من Figma `95:117` إلى `icons.ts` | ١٦×١٦، لون موروث، تسمية إلزامية |
| أعاين المكونات | `LUMA_GALLERY=1` أو `?gallery=1` على الويب | معرض لا يُشحن |
| أمسّ مظهر مساحة الكتابة | `src/editor.css` | §٩ أنماط النص |
| أعمل على نواة المحرر | `src/editor/` — اقرأ `src/editor/README.md` أولًا | §١ و§٧ |
| أغيّر تجميع التراجع | `src/editor/EditorCore.ts` → `UNDO_GROUP_DELAY_MS` | §٧ **ثابت** |
| أمسّ اللصق أو التنظيف | `src/editor/paste.ts` | §٧ النص المختلط |
| أصيغ نصًّا للواجهة فيه أرقام أو لاتيني | `src/lib/bidi.ts` | §٧ **ثابت** |
| أضيف صلاحية Tauri | `src-tauri/capabilities/default.json` | أقل صلاحية تكفي |
| أمسّ توقيت الحفظ | `src/lib/autosave.ts` | §٥ — [ADR ٠٠٠٥](decisions/0005-autosave-timing.md) |
| أربط المحرر بالتخزين | `src/lib/session.ts` | §٢ — المحرر لا يعرف الحفظ |
| أمسّ كتابة الملفات | `src-tauri/src/storage/atomic.rs` | §١٧ مبدأ ٢ — **لا تعدّل في مكانه** |
| أغيّر بنية المستند المخزَّن | `storage/model.rs` + خطوة في `migrate.rs` | §١٨ — الهجرة قبل التغيير |
| أمسّ اللقطات أو الاستعادة | `storage/revision.rs` | §٦ — [ADR ٠٠٠٦](decisions/0006-revision-format.md) |
| أمسّ ترتيب المكتبة أو الاستئناف | `storage/document.rs` → `list` و`most_recent` | §٦ — **الترتيب بآخر تعديل، والاستئناف بآخر فتح** |
| أمسّ الخطوط: تعدادًا أو استيرادًا أو تغطية | `src-tauri/src/fonts.rs` | §٨ — [ADR ٠٠٠٩](decisions/0009-fonts.md) |
| أُتيح خطًّا لنافذة العرض | `src/lib/font-faces.ts` | **التسجيل في النواة لا يكفي** — عملية العرض مستقلة |
| أضيف تفضيلًا أو أغيّر مداه | `src/lib/preferences.svelte.ts` | §١٥ — تُطبَّق وتُحفظ فورًا، ولا زر حفظ |
| أمسّ الآلة الكاتبة | `src/lib/typewriter.ts` | §٧ — **نطاق لا نقطة، وفوريّة لا انزلاق** |
| أمسّ المحرر المريح أو طبقاته | `src/components/EditorShell.svelte` (حالة `comfort`) | §٧ — غلاف واحد، لا شجرة ثانية |
| أضيف قسمًا للإعدادات | `src/lib/settings.ts` + `SettingsScreen.svelte` | §١٥ — لا قسم لميزة غير موجودة |
| أضيف أمرًا للتخزين | `src-tauri/src/commands.rs` | الواجهة تقرر متى، والنواة كيف |
| أمسّ ميزانية أداء أو أضيف واحدة | `src/dev/budgets.ts` + جدول §١٤ | [ADR ٠٠١١](decisions/0011-performance-budgets.md) — **يحرس تطابقهما اختبار** |
| أضيف قياسًا داخل التطبيق | `src/dev/selftest.ts` + أمرًا في `lib.rs` | القياس يُقارَن بميزانية أو لا يُقاس |
| أمسّ حالة خطأ أو سلوكها | الكود + [`error-behaviour.md`](error-behaviour.md) | السلوك **الفعلي** يُوثَّق، لا المرجوّ |
| أُخرج شجرة من مسار التركيز | سمة `inert` على جذرها | §١٣ — ومعه حاجزٌ ثانٍ لا يعتمد عليها |
| أضيف زوج لون أو أغيّر حالة نشِطة | المكوّن + `tests/tokens.test.ts` | الحارس يشتقّ الأزواج من الكود ويسقط على غير المفروض |
| أرفع أدنى إصدار macOS | `tauri.conf.json` | [ADR ٠٠١٢](decisions/0012-minimum-macos.md) — **جردٌ جديد لخصائص المنصة أولًا** |
| أمسّ الإطار أو تخطيط الشاشة | `src/components/EditorShell.svelte` | `IMPLEMENTATION.md` §١٠ — **يُبنى مرة واحدة** |
| أضيف لوحة جانبية أو مدخلًا لها | `src/lib/surfaces.ts` + `src/components/*Panel.svelte` | §١٠ — مدخل واحد وزر إغلاق وEsc |
| أمسّ المكتبة أو البحث فيها | `src/components/LibraryPanel.svelte` + `src/lib/library.ts` | `Luma.md` §٦ — [ADR ٠٠٠٨](decisions/0008-shell-and-surfaces.md) |
| أمسّ السجل أو المعاينة أو الاستعادة | `src/components/HistoryPanel.svelte` | §٩ — إجراء استعادة **واحد** في التذييل |
| أمسّ أدوات التنسيق الظاهرة | `src/components/SelectionToolbar.svelte` | §٥ — فقرة وH1 وH2 و«» فقط |
| أصيغ وقتًا في الواجهة | `src/lib/bidi.ts` → `sinceLabel` | §١٦ **ثابت** — **لا رموز محايدة في الوقت** |
| أمسّ موضع أزرار النافذة أو الطرف المحجوز | `src-tauri/src/lib.rs` → `window_controls_x` + `src/app.css` | [ADR ٠٠٠٣](decisions/0003-macos-rtl-window-chrome.md) |
| أضيف دورًا للكتل | `src/editor/schema.ts` + `blocks.ts` | `Luma.md` §٥ — **فقرة وH1 وH2 فقط** |

## نقاط حسّاسة

- **`luma_data_dir`** — مثبَّت على `~/Library/Application Support/Luma/` ولا يُشتق من معرّف الحزمة. المعرّف مؤقت ويُستبدل في المرحلة ٨؛ اشتقاق المسار منه يفقد بيانات المستخدم. اختبار في `src-tauri/src/lib.rs` يمنع الانحراف.
- **`src/app.css`** — لم تعد رموزًا مؤقتة: فيها متغيّرات التخطيط العامة التي لا تأتي من Figma، وأولها جانب أزرار النافذة. حارس المتغيّرات في `tests/tokens.test.ts` يقرأ منها كما يقرأ من طبقة الرموز.
- **الورقة لا يتغيّر عرضها عند فتح لوحة** — هذا هو ما يحفظ موضع التمرير، لا حيلةٌ تعيده بعد التغيير. تغييرُ العرض يعيد لفّ السطور فيتغيّر ارتفاع المحتوى، وعندها لا معنى لـ`scrollTop` القديم.
- **`{#if snippet}` صادقة دائمًا** — المقطع المُمرَّر موجود ولو لم يعرض شيئًا. عمود اللوحة يُشرَط بـ`activeSurface`، ووعاء التنبيه بـ`:empty`.
- **`__TAURI_INTERNALS__` هو فحص وجود الجسر** — لا نجاح استيراد `@tauri-apps/api`: الحزمة تُستورد في المتصفح بلا خطأ ثم يفشل أول `invoke`.
- **الخط المسجَّل في النواة لا تراه نافذة العرض** — WebKit يرسم في عملية محتوى مستقلة لا ترث تسجيل نطاق العملية. قيس: خط مسبار مسجَّل رُسم بعرض البديل نفسه (٣١٨px)، ومع `@font-face` بـ٣٦١px. يحرسه فحص `fonts-imported-usable`.
- **`@font-face` يُحمَّل عند أول استعمال** — القياس قبل `document.fonts.load()` يعطي مقاسات البديل لا الخط.
- **`pointermove` يُبثّ بإحداثيات لم تتغيّر بعد كل تمرير** — فتُقارَن الإحداثيات قبل عدّها حركة، وإلا لم يتراجع Zen أبدًا تحت مؤشر ساكن.
- **لا `behavior:"smooth"` في الآلة الكاتبة** — الأنيميشن يُلغى ويُعاد بدؤه مع كل حرف فيتخلّف العرض عن المؤشر.
- **جانب أزرار النافذة يُقاس ولا يُستنتج** — `NSApplication.userInterfaceLayoutDirection` أعطى «يسار» على جهاز تقع فيه أزرار Luma يمينًا، لأنها تُشتق من لغات الحزمة لا من لغة النظام.
- **`src/dev/`** و**`demo_mode`/`selftest_mode`** — أدوات تطوير، تُستورد ديناميكيًا فلا تدخل حزمة الإنتاج.
- **شبكة بعمودين صريحين وصفٍّ ضمني تُنتج صفّين.** الشبكة تضع العناصر بالترتيب ولا تعود إلى الوراء: عنصرٌ في العمود ٢ يدفع ما بعده في العمود ١ إلى **صفٍّ ثانٍ**. شريطا النافذة كانا كذلك — `grid-row: 1` صريح على كل عنصر.
- **`inert` هو ما يحمي النص خلف الشاشات، لا حجبُ البكسلات.** الشاشة تغطّي النافذة والمحرر خلفها يبقى في مسار `Tab` **ويستقبل الكتابة فعلًا** فتُحفظ في مستند لا يراه المستخدم. ومعه حاجزٌ ثانٍ (`setEditable`) لا يعتمد على دعم السمة.
- **`focus()` بعد رفع `inert` يحتاج `tick()`** — التغيير لا يُطبَّق على DOM فورًا، فيسقط الطلب صامتًا ويضيع التركيز إلى `<body>`.
- **`bind:group` في Svelte لا يكتب `name`** — فيرى المتصفح كل زر اختيار مجموعةً وحده: تموت الأسهم ويصير كل خيار محطة `Tab`. في قائمة خطوط النظام ٢٠٦ محطة.
- **`list-style: none` يُسقط دلالة القائمة في WebKit** — `role="list"` صريح يعيدها.
- **حقن الفشل فوق النواة لا يختبر النواة.** `LUMA_FAIL_WRITES` كان في طبقة الأوامر فيعود قبل أن تُلمس، الآن في `write_atomic` بـ`ENOSPC` حقيقي.
- **`set_var` يكسر الاختبارات المتوازية** — `cargo test` يوازي في خيوط، وحقن متغيّر بيئة في اختبارٍ أسقط خمسة أبرياء. الحقن للاختبار `thread_local`.
- **`flush()` الذي لا يُبلّغ يكذب على مستدعيه** — من ينتظره ليستبدل المحتوى يمضي وهو يظنّه محفوظًا. يعيد `boolean` الآن، والأنواع تفرض قراءته.
- **مزلاج الإغلاق يُفتح إن رفضت الواجهة** — وإلا مرّت المحاولة الثانية بلا حفظ فأُغلق التطبيق على النص نفسه الذي رُفض فقده.
- **قياس زمن الإقلاع من داخل الفحص الذاتي يقيس الفحص** — أعطى ٣٤٧٧ms والحقيقة ٤٨٦ms. العلامات تُلتقط في الإقلاع نفسه.
- **`Tab` على macOS يتبع إعداد النظام** «التنقل بلوحة المفاتيح» وهو مطفأ افتراضيًا. اختبار ترتيب التركيز على WebKit يقيس الإعداد لا البنية — ولذلك يُقاس على Chromium.
- **أداة تطوير تكتب عبر مسار الإدخال تكتب فوق مستند المستخدم.** الفحص الذاتي والعرض البصري كانا يفعلان: `insertText` يمرّ بـ`onChange` ثم الجلسة ثم الحفظ التلقائي، فيُكتب نصّ الأداة فوق المستند المستأنف. الآن الفحص يفصل الجلسة ولا يستأنف، والعرض لا يمرّر مستنده على الجلسة. يحرسه فحص `selftest-isolation`.
- **`cleanup_selftest` لا يحذف إلا ما يبدأ بـ`selftest-`** — لا يوجد أمر حذف عام في Luma، و«لا حذف» قرارٌ في `Luma.md` §٦.
- **حدود `src/editor/`** — يحرسها `tests/editor-boundaries.test.ts`. لا تستورد فيها من التخزين أو Tauri أو المكتبة: الاختبار يسقط.
- **`write_atomic`** — ملف مؤقت في **المجلد نفسه** ثم `fsync` ثم `rename`. المؤقت في `/tmp` يُبطل الذرّية لأن `rename` غير ذرّي عبر أنظمة ملفات.
- **سقف الحفظ ٥ ثوانٍ لا يُعاد ضبطه مع كل حرف** — هو ما يحدّ الخسارة عند الإغلاق القسري. إعادة ضبطه تجعله نسخة أطول من مهلة السكون.
- **لقطات `beforeRestore` لا تُقصّ أبدًا** — شبكة أمان الاستعادة.
- **`LUMA_FAIL_WRITES=1`** يحقن فشل كتابة لاختبار مسار الفشل.
- **`requestAnimationFrame` يتوقف للنوافذ المحجوبة على macOS.** أي انتظار للرسم داخل أداة فحص يحتاج مهلة احتياطية، وإلا توقف الفحص بلا خطأ ولا تقرير. `src/dev/selftest.ts` فيه المهلة، وسكربت التشغيل يُفعّل النافذة.
- **مخرجات `src/tokens/` مولَّدة** — تُحرَّر `source.json` ثم يُعاد التوليد. تحرير `tokens.css` يدويًا يُمحى.
- **`var(--…)` غير معرَّف يسقط صامتًا** — لا خطأ ولا تحذير، وينهار المقاس. يحرسه اختبار في `tests/tokens.test.ts`.
- **مساحة الكتابة: مُمرِّر واحد فقط** — `.scroller`. الورقة تنمو مع محتواها ولا تُمرَّر بنفسها. لا تجعل الحاوية `grid` بـ`align-content: stretch`: يُشدّ الصف إلى الارتفاع المرئي فتتوقف خلفية الورقة عند حدود النافذة بينما يستمر النص خارجها.
- **`user-select: none` لا يعلو منطقة الكتابة أبدًا** — يُوضع على `.luma-chrome` وحدها. سلفٌ `none` فوق محرر `text` يجعل WebKit يرسم التحديد مستطيلًا لا يتبع الأسطر.
- **لا `-webkit-user-modify`** — يغيّر حبيبة التحديد فيلتقط النقر المزدوج الفقرة كاملة.
- **لا تعطيل الروابط (`liga`) في المحرر** — CSS الرسمي لـProseMirror يعطّلها لمشكلة مؤشر لاتينية، وتعطيلها يفكّ «لا» وأمثالها في العربية.
- **حدّ الورقة ليس زينة** — بدونه تذوب في «ليل» حيث `surface/paper` و`surface/canvas` متقاربان.
- **`#luma` عمود بارتفاع النافذة** — بدونه لا يجد `flex:1` أبًا محدَّدًا فيُمرَّر الجسم كله ويهرب شريط العنوان.
- **التراجع مكدّس واحد** — بند القائمة يبثّ `luma://menu` والواجهة تنفّذ. لا تُعِده `PredefinedMenuItem::undo`: يوقظ مدير تراجع WebKit فيتنازع مكدّسان على النص.

## الاختبار

| الطبقة | الأمر | ما لا يغطيه |
|---|---|---|
| Rust | `npm run check:rust` | — |
| الأنواع | `npm run check` | — |
| الوحدات | `npm run test` | — |
| داخل `Luma.app` | `npm run selftest` | **لا WebDriver لـWKWebView على macOS** — يخرج بحالة غير صفرية عند سقوط بند |
| Rust | `npm run test:rust` | ٤٩ اختبارًا — **دخلت `verify` في المرحلة ٧** |
| الوصول | `npm run test:e2e -- access` | VoiceOver وتكبير نص النظام يدويان |
| طبقة الويب | `npm run test:e2e` | ليس WKWebView — كشف انحدار فقط |
| مشهد بصري داخل التطبيق | `LUMA_STAGE=library\|history\|preview\|count\|comfort\|settings[:قسم]\|fonts` | يفتح المشهد ليُلتقط شكله — **لا يغني عن التجربة اليدوية** |
| شاشة الإعدادات على الويب | `?settings=1` | تخطيطها وقواعدها؛ الخطوط والحفظ داخل التطبيق |

ما لا تحكم عليه الأتمتة إطلاقًا: الإملاء الأصلي، قوائم السياق، VoiceOver، تكبير نص النظام، سلوك النافذة، الإحساس العام للكتابة. يُختبر يدويًا داخل التطبيق.

## الأدلة والتقارير

| الملف | ماذا فيه |
|---|---|
| [`quality-report.md`](quality-report.md) | §١٣ بندًا بندًا — آليّ مقابل يدويّ |
| [`error-behaviour.md`](error-behaviour.md) | سلوك عشر حالات خطأ **كما هو فعلًا** |
| [`release-checklist.md`](release-checklist.md) | ما لا تحكم عليه الأتمتة — قبل كل إصدار |
| [`arabic-battery.md`](arabic-battery.md) | بطارية العربية عبر المراحل |
| `evidence/` | تقارير الفحص الذاتي ولقطات التحقق ونتائج Lighthouse |
