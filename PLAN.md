# Luma — خطة التنفيذ

المسار التنفيذي من حالة المشروع الحالية حتى الإطلاق.

تعريف المنتج ونطاقه وسلوكه في [Luma.md](Luma.md). الأساس التقني وقواعده في [IMPLEMENTATION.md](IMPLEMENTATION.md). هذه الوثيقة **لا تعيد** ما فيهما ولا تنافسهما: عند أي تعارض، الوثيقتان أعلى.

الأوسمة كما في `Luma.md`: **ثابت** التزام لا يُساوَم عليه · **توجه** اقتراح عملي قابل للاستبدال · **مفتوح** يحتاج قياسًا أو قرارًا.

---

## كيف تُستخدم هذه الوثيقة

لكل مرحلة رقم واسم ثابتان لا يتغيّران. عند قول **«ابدأ المرحلة الأولى»** — أو أي رقم آخر — يجري التالي بلا إعادة تخطيط وبلا طلب شرح:

1. فحص حالة المشروع الفعلية على القرص.
2. تنفيذ نطاق تلك المرحلة كما هو مكتوب هنا.
3. اتخاذ القرارات التنفيذية المعتادة داخل النطاق بلا استئذان.
4. اختبار المخرَج فعليًا وفق معيار الاكتمال.
5. تحديث آلية فهم المشروع.
6. عدم إعلان الاكتمال قبل التحقق، وعدم الانتقال إلى المرحلة التالية إلا بطلب صريح.

---

## حالة المشروع الآن

| البند | الحالة |
|---|---|
| الكود | لا يوجد |
| المستودع | غير مُهيَّأ، ولا git |
| الوثائق | `Luma.md` و`IMPLEMENTATION.md` مكتملتان ومتوافقتان مع الاتجاه الحالي |
| التصميم | ملف Figma `7CDWsUiMz0xae1CRwvpmPY` — عشرون صفحة، مفهرسة أدناه بمعرّفات العقد |
| مكتبة المكونات | الصفحة ١٧ مكتبة حقيقية لا لوحة عرض: ٢٩ مكوّنًا أكثرها مجموعات بحالات موثّقة، وكل لون فيها مربوط بمتغيّر |
| البيئة | macOS 26.6 على arm64 — Xcode 26.6 — Rust 1.96 — Node 24 |
| التوقيع | شهادة تطوير متاحة. `Developer ID` تُستخرج عند المرحلة ٨، ولا تعوق ما قبلها |

---

## خريطة Figma

الملف `7CDWsUiMz0xae1CRwvpmPY`. تُفتح الصفحة بمعرّف عقدها مباشرة بلا بحث.

| # | الصفحة | `node-id` | تُستخدم في |
|---|---|---|---|
| ٠٠ | نواة العلامة | `22-2` | ١، ٨ |
| ٠١ | أساس العلامة | `0-1` | ٨ |
| ٠٢ | مفهوم الشعار | `2-2` | مرجع |
| ٠٣ | نظام الشعار | `2-3` | ٨ |
| ٠٤ | أيقونة التطبيق | `2-4` | ١، ٨ |
| ٠٥ | نظام الألوان | `2-5` | ٤ |
| ٠٦ | النظام الطباعي | `6-3` | ٢، ٤ |
| ٠٧ | اللغة البصرية | `6-4` | ٤ |
| ٠٨ | مبادئ تجربة المنتج | `6-5` | مرجع |
| ٠٩ | المحرر الرئيسي | `6-6` | ٢، ٥ |
| ١٠ | المكتبة الجانبية | `6-7` | ٥ |
| ١١ | المحرر المريح | `6-1094` | ٦ |
| ١٢ | مساحة الأفكار | `6-1095` | خارج النطاق |
| ١٣ | الحفظ والسجل الزمني | `6-1096` | ٣، ٥ |
| ١٤ | الصوت والبيئة | `6-1097` | خارج النطاق |
| ١٥ | الإملاء الصوتي | `6-1098` | خارج النطاق |
| ١٦ | الإعدادات | `6-2111` | ٦ |
| ١٧ | نظام المكونات | `6-2112` | ٤، ٥، ٦ |
| ١٨ | حالات الاستخدام | `6-2113` | **عرض فقط — لا متطلبات** |
| ١٩ | النموذج التفاعلي | `143-73` | ٥، ٧ |

**قاعدة القراءة:** وصف كل مكوّن في الصفحة ١٧ يحمل قواعده وحدوده، ويُقرأ قبل بنائه. وما في الصفحة ١٨ مادة تقديمية لا تتحول إلى قصص مستخدم ولا كيانات بيانات — `Luma.md` §١٩.

---

## ثوابت تحكم كل المراحل

لا تُعاد كتابتها في كل مرحلة. **مرحلة لا تحققها كلها ليست مكتملة.**

1. **التحقق داخل `Luma.app` مبنية.** لا معاينة متصفح، ولا خادم تطوير، ولا لقطة، ولا وصف. تُجرَّب بالماوس ولوحة المفاتيح معًا.
2. **المبادئ الحاكمة العشرة** في `IMPLEMENTATION.md` §١٧ تُراجَع قبل إعلان الاكتمال.
3. **اختبار آلي** لكل طبقة تقبله — بما فيه أدوات فحص طبقة الويب أدناه — وقائمة تحقق يدوية مكتوبة لما لا يقبله.
4. **آلية فهم المشروع محدَّثة** — `docs/MAP.md` وADR لكل مسألة حُسمت.
5. **مصالحة الوثيقة** — أي قسم في `IMPLEMENTATION.md` تغيّر واقعه يُحدَّث في المرحلة نفسها، لا لاحقًا.
6. **لا ميزة خارج `Luma.md`.** ما ليس في المنتج لا يدخل الكود، ولو بدا مجانيًا.
7. **سلامة النص أولًا.** عند أي مفاضلة بين خاصية ثانوية وسلامة النص أو الحفظ أو تجربة الكتابة، تسقط الخاصية الثانوية.

---

## بطارية العربية

قائمة ثابتة تُكتب مرة في `docs/arabic-battery.md` وتُشغَّل **داخل `Luma.app`** في نهاية المراحل ١ و٢ و٣ و٦ و٧. لا تُعاد صياغتها ولا يُحذف منها بند بلا قرار مسجَّل.

**المؤشر والتحديد**
مؤشر الكتابة عند طرف السطر الأيسر — النقر المزدوج يحدد كلمة عربية كاملة — التحديد بالسحب متصل بصريًا بلا فجوات — التحديد عبر مقطع مختلط «كتبت hello للعالم» بلا قفز — `Home` و`End` يصلان بداية السطر ونهايته منطقيًا — الأسهم تتحرك كأعراف macOS.

**الإدخال والحذف**
تشكيل الحروف واتصالها في «بسم الله الرحمن الرحيم» — `Backspace` يحذف عنقودًا رسوميًا واحدًا لا بايتًا، مع التشكيل والهمزات — تراجع واحد يزيل دفقة كتابة لا حرفًا — إدراج من عارض المحارف والرموز التعبيرية.

**التبادل مع النظام**
نسخ من Luma ولصق في TextEdit ينتج النص نفسه بلا رموز اتجاه مدسوسة — لصق نص عربي من Safari أو Notes بلا تنسيق وبلا فساد اتجاه — قائمة السياق الأصلية تظهر بعناصرها (بحث، تدقيق) — الإملاء الأصلي في macOS يعمل داخل مساحة النص.

**العرض والطباعة**
تتبّع صفر على كل نص عربي بلا استثناء — ارتفاع السطر مصرَّح به لا متروك للتلقائي — لا نص عربي دون ١٢ نقطة — الأرقام العربية الهندية في نصوص الواجهة — لا فاصل «·» ملاصق لرقم عربي هندي لأنه يُقرأ صفرًا — لا امتداد ملف لاتيني داخل نص عربي.

**الحِمل والاستقرار**
مستند ٥ آلاف كلمة ومستند ٢٠ ألف كلمة: زمن ظهور الحرف وسلاسة التمرير — تمركز الآلة الكاتبة بلا اهتزاز — تغيير الثيم أو الخط أو الحجم يحفظ موضع المؤشر والتمرير.

**الوصول**
VoiceOver يقرأ النص العربي ويعلن مساحة النص منطقةَ تحرير — ترتيب التركيز بلوحة المفاتيح منطقي من اليمين إلى اليسار.

---

## أدوات فحص طبقة الويب

متاحتان عبر MCP وتُستخدمان حيث تفيدان، بحدودهما معلنة.

| الأداة | ما تصلح له في Luma | حدّها |
|---|---|---|
| **Playwright** | سلوك الواجهة وشجرة الوصول والتنقل بلوحة المفاتيح، والتحديد والمؤشر، ولقطات مرجعية للمكونات والثيمات، واختبارات انحدار سريعة على كل تغيير | لا يقود نافذة `Luma.app` ولا WKWebView. يعمل على طبقة الويب في متصفحه هو |
| **Lighthouse** | تدقيق الوصول والتباين والأداء على طبقة الويب، وكشف الانحدار بين المراحل | يقيس صفحة ويب لا تطبيق macOS. أرقام أدائه ليست ميزانيات المنتج |

**القاعدة الحاكمة** **ثابت**

- تُشغَّلان على خادم التطوير أو البناء الثابت، ويُفضَّل محرك WebKit في Playwright لأنه الأقرب إلى نافذة العرض الفعلية.
- **نتيجة خضراء منهما لا تعني اجتياز المرحلة.** لا مرحلة تُعلَن مكتملة إلا بعد تجربتها داخل `Luma.app` مبنية.
- ما لا تستطيعان الحكم عليه أصلًا — الإملاء الأصلي، قوائم السياق، VoiceOver، تكبير نص النظام، تقليل الحركة على مستوى النظام، سلوك النافذة والأزرار، الإحساس العام للكتابة — يُختبر يدويًا داخل التطبيق حصرًا.
- تعارض نتيجتهما مع سلوك التطبيق الفعلي يُحسم لصالح التطبيق، وتُصحَّح الأداة أو يُسجَّل الفرق.

---

## آلية فهم المشروع

ثلاثة ملفات فقط. تنشأ في المرحلة ١ وتتطور مع كل مرحلة. الغرض: أن يصل أي وكيل إلى ملفات مهمته بلا قراءة المستودع كله، **بلا أن تصبح مصدر حقيقة بديلًا**.

| الملف | ما فيه | ما ليس فيه |
|---|---|---|
| `CLAUDE.md` | نقطة الدخول: ما هو Luma، الوثيقتان المرجعيتان، الحزمة، أوامر البناء والاختبار، خريطة المجلدات | لا قواعد منتج، ولا تفاصيل تصميم |
| `docs/MAP.md` | فهرس «مهمة ← ملفات واختبارات» | **مؤشرات فقط. ولا قاعدة واحدة.** |
| `docs/decisions/` | قرار واحد في ملف صغير لكل مسألة حُسمت من `Luma.md` §٢٠ أو `IMPLEMENTATION.md` §١٨ | لا نقاش مفتوح، ولا خيارات مؤجَّلة |

كل ADR يحمل: القرار، السياق، البدائل المرفوضة ولماذا، الأثر، وأي قسم من الوثيقتين يُحدَّث بسببه.

**قاعدة الحماية:** أي جملة تصلح لأن تكون قاعدة تنتمي إلى `Luma.md` أو `IMPLEMENTATION.md`، وMAP.md يشير إليها فقط. اليوم الذي تُقرأ فيه MAP.md بدلًا من الوثيقتين هو اليوم الذي فشلت فيه.

---

# المراحل

## المرحلة ١ — بوابة القدرات · `capability-gate`

**الهدف.** حسم المسألتين ١ و٢ في `IMPLEMENTATION.md` §١٨: هل يصمد Tauri كغلاف، وعلى أي أساس تُبنى نواة تحرير النص. المخرَج الحقيقي **حُكم مسنود بدليل**، والكود أداة القياس لا الغاية.

**لماذا أولًا.** كل ما بعدها مبني عليها. اكتشاف عجز الغلاف بعد بناء الأسطح والثيمات كلفته المشروع كله، واكتشافه الآن كلفته مرحلة واحدة.

**النطاق.**
- تهيئة المستودع: git، `.gitignore`، `.nvmrc`، بنية المجلدات.
- هيكل Tauri أدنى: نواة Rust، نافذة عرض، بناء ينتج `Luma.app`.
- **معرّف حزمة مؤقت للتطوير** — `dev.luma.app`. المعرّف النهائي يُثبَّت في المرحلة ٨ مع شهادة التوزيع.
- **مسار التخزين يُثبَّت صراحةً** على `~/Library/Application Support/Luma/` مستقلًّا عن معرّف الحزمة، لا مشتقًّا منه. هذا ما يجعل تبديل المعرّف لاحقًا بلا أثر على بيانات المستخدم — وهو شرط لا خيار.
- إعدادات النافذة: شريط عنوان شفاف يُبقي أزرار النظام يسارًا بلا إعادة رسم، مقاس افتراضي ١٢٨٠×٨٦٠ وأدنى ٩٦٠×٦٤٠، سياسة أمان مغلقة بلا أي منفذ شبكة.
- القائمة الأصلية لـmacOS بعناصر التحرير القياسية — شرط أن تصل اختصارات النص إلى مساحة الكتابة.
- Cairo وAlmarai مدمجان ويعملان بلا اتصال شبكة.
- اتجاه RTL على مستوى التطبيق وخصائص تخطيط منطقية كقاعدة.
- **مقارنة أسس نواة المحرر** — المرشحون: محرر ذو مخطط كتل، ومحرر ذو نموذج شجري، ومساحة تحرير خام. يُحكم عليهم بقدرات `IMPLEMENTATION.md` §١ الخمس وسلوكيات §٧، لا بالانطباع.
- قياس أداء على مستندات عربية اصطناعية بـ٥ آلاف و٢٠ ألف كلمة.
- `CLAUDE.md` و`docs/MAP.md` و`docs/arabic-battery.md` وأول ADR.

**الاعتماديات.** لا شيء.

**المخرجات.**
- مستودع يبني `Luma.app` بأمر واحد.
- تقرير بطارية العربية بندًا بندًا على كل مرشح، بنتيجة ودليل.
- جدول قياس أولي: زمن الفتح حتى مؤشر قابل للكتابة، زمن ظهور الحرف، سلوك التمرير على المستندين.
- **ADR ٠٠٠١** — تثبيت الغلاف أو مراجعته، بالدليل.
- **ADR ٠٠٠٢** — أساس نواة المحرر، والبدائل المرفوضة ولماذا.
- تحديث `IMPLEMENTATION.md` §١ و§١٨ بما استقر.

**معيار الاكتمال.**
- `Luma.app` تُفتح من Finder، وأزرار النظام يسارًا، وسحب النافذة يعمل، ولا خطأ عند الفتح والإغلاق وإعادة الفتح.
- قدرات §١ الخمس **مُثبَتة عمليًا** على الأساس المختار، لا موصوفة.
- ما يجب أن يأتي من المنصة (قوائم السياق، الإملاء الأصلي، اختصارات التحرير، VoiceOver) يعمل بلا بديل مكتوب يدويًا.
- كتابة فقرة عربية طويلة داخل التطبيق بلا تأخر محسوس.
- حُكم مكتوب: **يُمضى بـTauri** أو **يُراجَع** — وإن كان الثاني، تتوقف الخطة عند هذا الحد وتُعاد المراحل ٢ فما بعد على الأساس الجديد.

---

## المرحلة ٢ — نواة المحرر · `editor-core`

**الهدف.** نواة تحرير حقيقية مستقلة، تجتاز بطارية العربية كاملة، ولا تعرف شيئًا عن المكتبة ولا الحفظ ولا الإعدادات.

**النطاق.**
- `EditorCore` على الأساس المُقَر في المرحلة ١، بحدوده في `IMPLEMENTATION.md` §٢.
- نموذج `Block` مصدرًا وحيدًا للمحتوى: `id` و`role` و`text` و`marks`. الأدوار `body` وh1 وh2 فقط، و`marks` فارغة.
- تسلسل الكتل وتحويلها ذهابًا وإيابًا مع مستند المحرر بلا فقد ولا إعادة ترتيب.
- الاتجاه الأساسي مصرَّح به على مساحة النص، والنص المختلط قابل للتحرير.
- عزل المقاطع اللاتينية داخل سلاسل الواجهة العربية بمحدِّدات الاتجاه، أو تجنّب الإدراج أصلًا.
- تجميع التراجع بحسب دفقة الكتابة.
- ربط عناصر قائمة التحرير الأصلية بالمحرر، واللصق نصًّا عاديًا يُنظَّف إلى كتل.
- عرض بالحد الأدنى: Almarai ١٩/٣٦، عمود ٦٤٠، سطح واحد مؤقت. لا ثيمات بعد.

**الاعتماديات.** ١.

**المخرجات.**
- `EditorCore` مستقل، وحدوده مفروضة لا موصوفة.
- اختبارات آلية للتحويل والتسلسل وتجميع التراجع وتنظيف اللصق.
- **Playwright:** مجموعة انحدار لسلوك المؤشر والتحديد والتراجع واللصق وموضع المؤشر بعد تغيير الخط — تُشغَّل على كل تغيير لاحق في المحرر.
- بطارية العربية مُشغَّلة كاملة داخل `Luma.app` بنتيجة موثّقة.
- مصالحة `IMPLEMENTATION.md` §٧.

**معيار الاكتمال.**
- كل بنود بطارية العربية تمر، أو يُسجَّل البند الساقط كقيد معروف بقرار صريح ومكتوب.
- كتابة ٥٠٠ كلمة عربية متصلة بلا تأخر محسوس.
- تراجع واحد يزيل جملة لا حرفًا.
- نسخ إلى TextEdit ولصق منه يعملان بلا فساد اتجاه.
- تغيير الخط أو الحجم لا يفقد موضع المؤشر.
- محاولة استدعاء أي شيء من المكتبة أو التخزين داخل `EditorCore` تفشل بنيويًا، لا اتفاقًا.

---

## المرحلة ٣ — الاستمرارية والحفظ · `persistence`

**الهدف.** نص لا يضيع. حفظ تلقائي دائم لا يحجب الإدخال ولا يحرّك المؤشر، وتعافٍ بعد إغلاق قسري.

**النطاق.**
- طبقة التخزين في النواة الأصلية وفق `IMPLEMENTATION.md` §٤: ملف لكل مستند، كتابة ذرّية دائمًا، مخزن تفضيلات منفصل، مسار `~/Library/Application Support/Luma/`.
- الحفظ التلقائي بمسار §٥: تجميع ٧٠٠ مللي ثانية، سقف ٥ ثوانٍ أثناء الكتابة المتصلة، كتابة فورية عند فقد التركيز أو الإغلاق أو تبديل المستند. الإغلاق يُؤجَّل حتى تنتهي الكتابة الأخيرة.
- حالة الحفظ بحالاتها الثلاث: أيقونة ونص ولون معًا، تُعلَن لقارئ الشاشة بلا سحب التركيز، ولا تتحول إلى شريط دائم.
- الفشل: البُفر يبقى، ولا يُستبدل ملف سليم بجزئي، وإعادة محاولة بتباعد متزايد، وفرصة استرجاع صريحة عند الإغلاق.
- `RevisionStore` بحد أدنى عملي: محفّزات §٦ الأربعة، ومعاينة قراءة فقط، ولقطة قبل الاستعادة، وإجراء استعادة واحد.
- `schemaVersion` ومسار هجرة مختبر من أول يوم.
- حسم المسائل ١ و٢ و٣ في `Luma.md` §٢٠: سلوك إعادة الفتح، ومصير عنوان «بدون عنوان»، ومصير مساحة فُتحت وأُغلقت بلا محتوى. *(حسم المسألتين ١ و٣ نُقض لاحقًا — ٢٥ أغسطس ٢٠٢٦، ADR ٠٠١٧.)*

**الاعتماديات.** ٢.

**المخرجات.**
- اختبارات للكتابة الذرّية ومقاطعتها، وللملف التالف، وللهجرة بين إصدارات المخطط.
- سيناريو تعافٍ موثّق: إنهاء قسري أثناء الكتابة ثم إعادة فتح.
- ADR لصيغة اللقطة ومدة الاحتفاظ وحدودها، مبنيًا على قياس مستند ٢٠ ألف كلمة لا على تقدير.
- ADR لكل مسألة حُسمت من `Luma.md` §٢٠.
- مصالحة §٤ و§٥ و§٦.

**معيار الاكتمال.**
- إنهاء قسري أثناء الكتابة لا يفقد أكثر من نافذة التجميع، وإعادة الفتح تستأنف النص. *(الاستئناف أُلغي لاحقًا — ADR ٠٠١٧: النص محفوظ ويُفتح من المكتبة.)*
- لا زر حفظ ولا اختصار حفظ يدوي في أي مسار.
- إدخال متصل لدقيقتين بلا قطع وبلا حركة للمؤشر.
- محاكاة فشل الكتابة تُظهر «تعذّر الحفظ» بأيقونة ونص، والنص يبقى قابلًا للكتابة والمحتوى في الذاكرة.
- استعادة نسخة تحفظ الحالة الحالية أولًا، وتظهر الاثنتان في السجل.

---

## المرحلة ٤ — الرموز والثيمات · `tokens-themes`

**الهدف.** كل لون وقياس ونمط نص رمزٌ مشتق من Figma، والثيمات الخمسة تعمل داخل التطبيق.

**النطاق.**
- استخراج المجموعات الثلاث من Figma: `Theme` بـ٢٤ لونًا في خمسة أوضاع، `Space` بـ١٤ خطوة، `Radius` بستة.
- طبقة رموز واحدة مولَّدة من مصدر واحد، بأسماء تطابق Figma واحدًا لواحد وفق §٩.
- أنماط النص: الواجهة بـCairo، والمحرر بـAlmarai، بارتفاعات سطر صريحة وتتبّع صفر.
- الأيقونات الاثنتان والعشرون (`95:117`) إلى مجموعة أصول موحّدة: شبكة ١٦×١٦، سماكة ١٫٤، لون موروث. **لا أيقونة بلا تسمية وصفية.**
- المكونات الأساسية من الصفحة ١٧ بحالاتها كما هي موثّقة فيها، لا باجتهاد:

| المكوّن | العقدة | الحالات |
|---|---|---|
| زر · Button | `92:125` | ٤ أنواع × ٦ حالات |
| زر أيقونة · Icon Button | `98:91` | ٦ حالات (منها «نشط») |
| زر المحرر المريح | `103:106` | ٣ حالات |
| مفتاح · Toggle | `98:104` | قيمتان × ٣ حالات |
| خانة اختيار · Checkbox | `99:94` | ٣ قيم (منها «جزئي») × ٣ حالات |
| زر اختيار · Radio | `99:104` | قيمتان × ٣ حالات |
| منزلق · Slider | `99:117` | ٣ حالات |
| حقل نص · Text Field | `100:111` | ٨ حالات |
| حقل بحث · Search Field | `111:223` | ٣ حالات |
| حالة الحفظ · Save Status | `100:124` | ٣ حالات |
| شارة · Badge | `100:135` | ٥ أنواع |
| تنبيه · Alert | `104:118` | ٤ أنواع |
| إشعار عابر · Toast | `104:137` | ٣ أنواع |
| ترويسة لوحة · Panel Header | `103:88` | مكوّن واحد يفرض زر الإغلاق |
| صف إعداد · Settings Row | `114:382` | ٣ أنواع |
| صف تنقل · Nav Row | `114:398` | ٣ حالات |
| بطاقة ثيم · Theme Card | `114:489` | ٥ ثيمات × حالتين |

- **مؤجَّل إلى مرحلته:** صف مستند ونقطة سجل وشريط التحديد في المرحلة ٥. وخارج نطاق الخطة: بطاقة فكرة وشريط الإملاء.
- **لا يُبنى:** `أزرار النافذة · Window Controls` (`103:84`) — يُستخدم شريط النافذة الأصلي.
- عناصر التحكم تُبنى على عناصر المنصة الأصلية بنمط مخصص، لا مرسومة.
- كل حالة تفاعلية تُنفَّذ حالةً في الكود لا لقطة تصميم: `hover` و`active` و`focus-visible` و`disabled` و`loading` — قاعدة التسليم في الصفحة ١٧.
- **قسم التحقق من الثيم الداكن** (`149:366`) مرجعٌ للمقارنة: هو المكونات نفسها بوضع «ليل» مثبَّتًا على الحاوية، لا نسخة مرسومة.
- قواعد الأدوار في §٩ مفروضة: `accent-graphic` لا يقع خلف نص، والصف المحدد `accent-subtle` مع `accent-text`، والزر الممتلئ `accent-text` مع `text-on-accent`، و`border-subtle` لا يعرّف عنصر تحكم وحده.
- حلقة التركيز إطارًا ٢px بإزاحة ٢px، لا تُلغى أبدًا.
- تطبيق الثيمات على ما بُني في المرحلتين ٢ و٣.

**الاعتماديات.** ٣. مرجعها في Figma الصفحتان ١٧ و٠٥.

**المخرجات.**
- مصدر رموز واحد ومولّد، وفحص آلي يمنع أي لون مكتوب يدويًا.
- معرض مكونات داخلي لا يُشحن: كل مكوّن × كل حالة × خمسة ثيمات.
- فحص تباين آلي لكل زوج رمز في الثيمات الخمسة.
- **Playwright:** لقطات مرجعية للمعرض في الثيمات الخمسة، تكشف انحدار أي مكوّن في أي ثيم لاحقًا.
- **Lighthouse:** تدقيق وصول على المعرض يؤكد التباين وأدوار العناصر، مقارنًا بنتيجة الفحص الآلي.
- مصالحة §٩.

**معيار الاكتمال.**
- تبديل الثيم داخل `Luma.app` يغيّر كل السطوح فورًا بلا وميض وبلا فقد موضع التمرير أو المؤشر.
- فحص «لا لون حرفي» يمر على المستودع كله.
- فحص التباين يمر، أو كل استثناء مسجَّل بقرار.
- مقارنة الثيمات الخمسة بصريًا مع Figma تجري **داخل التطبيق**.

---

## المرحلة ٥ — الإطار والأسطح · `shell-surfaces`

**الهدف.** بناء إطار المحرر مرة واحدة، وفتح اللوحات كأعمدة لا كإعادة تصميم.

**النطاق.**
- الإطار وفق §٩ و§١٠: شريط نافذة ٤٨ بأزرار النظام يسارًا وعنوان المستند وسطًا وحالة الحفظ يمينًا، شريط أسطح ٥٢ بمداخله على اليمين، ورقة ٧٦٠ بحشوة جانبية ٦٠، عمود نص ٦٤٠.
- **قاعدة اللوحة الواحدة:** فتح لوحة يضيف عمودًا ٣٢٠ ويعيد تمركز عمود النص فيما بقي. لا يتغيّر المستند ولا الورقة ولا شريط النافذة ولا موضع المؤشر.
- سلوك النافذة الضيقة: يتقلّص عمود النص نحو ٥٢٠ قبل أن تتقلّص الورقة.
- **إطار المحرر · Editor Shell** (`106:97`) يُبنى مرة واحدة، وكل شاشة منتج نسخة منه.
- المكتبة (`110:195`): مخفية افتراضيًا، ثلاث حالات — ممتلئة، فارغة، لا نتائج. صف المستند (`105:121`) بأربع حالات، العنوان يمين والوقت يسار، والصف المحدد `accent-subtle` مع `accent-text` وشريط على الحافة اليمنى. فتح مستند يحفظ الحالي أولًا، والإغلاق لا يفقد المؤشر ولا التحديد.
- السجل (`112:339`): ثلاث حالات للوحة، ونقطة السجل (`105:166`) بأربع حالات منها «النسخة الحالية» و«قيد المعاينة». المعاينة قراءة فقط بلا مؤشر حفظ، وإجراء استعادة واحد معطَّل عند تحديد النسخة الحالية.
- شريط التحديد (`113:375`): حالتان، يظهر مع التحديد فقط ويختفي عند استئناف الكتابة. فقرة وh1 وh2 وعلامة الاقتباس العربية. **لا مائل ولا شريط تنسيق دائم.**
- رقاقة عداد الكلمات مخفية افتراضيًا.
- زر المحرر المريح عنصرًا عائمًا، بلا سلوكه بعد.
- شريط الأسطح يعرض المداخل المنفَّذة فقط، والمكوّن يحتمل الخمسة بلا تعديل لاحق.
- `Esc` يغلق أي لوحة، ولكل لوحة زر إغلاق ظاهر، ولا لوحة تحبس التركيز ولا تحجب حامله.
- حسم المسألة ٤ في `Luma.md` §٢٠: الحد الأدنى لعمليات المكتبة.

**الاعتماديات.** ٤. مرجعها في Figma الصفحات ٠٩–١٦.

**المخرجات.**
- الإطار وكل لوحة بكل حالاتها المطلوبة في §١٠.
- **Playwright:** فتح كل لوحة وإغلاقها من مدخلها ومن زرها ومن `Esc`، مع التحقق من بقاء موضع المؤشر والتحديد والتمرير، ومن أن التركيز لا يُحبس داخل أي لوحة.
- مصالحة §١٠ و§١٢.

**معيار الاكتمال.**
- فتح أي لوحة داخل `Luma.app` لا يزيح الورقة ولا يفقد المؤشر ولا التحديد ولا موضع التمرير.
- كل لوحة تعمل بالماوس وبلوحة المفاتيح معًا.
- لا شكل في أي شاشة غير موجود كمكوّن في الصفحة ١٧ — ونقصه يُعالَج في المكتبة لا باستثناء في الشاشة.

---

## المرحلة ٦ — المحرر المريح والتخصيص · `comfort-preferences`

**الهدف.** الطبقات الثلاث تعمل بمخرج ثابت، والإعدادات تُطبَّق وتُحفظ فورًا، والخطوط تعمل من مصادرها الثلاثة.

**النطاق.**
- المحرر المريح غلافًا واحدًا: تختفي اللوحات والقوائم والأدوات، وتتسع مساحة النص، ويبقى النص والمؤشر ووسيلة الخروج.
- **الآلة الكاتبة:** تمرير يُبقي السطر النشط في نطاق وسطي مريح، بحشوة تكفي السطر الأول والأخير، هادئ وفوري عند تقليل الحركة.
- **التركيز:** تخفيت النطاقات غير النشطة بطبقة عرض مؤقتة بلون رمز مقروء، والمحيط يبقى مقروءًا لا شبه مخفي.
- **Zen:** تراجع الواجهة أثناء الكتابة وعودتها بنمط ثابت.
- الخروج: زر خافت ظاهر واختصار متوافق مع أعراف macOS، لا يعتمد على حركة المؤشر وحدها، ولا يغيّر موضع المؤشر ولا حالة النص.
- الإعدادات بأقسامها الستة في `Luma.md` §١٥، تُطبَّق وتُحفظ فورًا بلا زر حفظ.
- الخطوط: تعداد خطوط النظام بأسمائها الصحيحة، واستيراد ملف ونسخه وإتاحته، وفحص التغطية العربية بقراءة جدول المحارف، وسلسلة الرجوع الآمن إلى Almarai مع إشعار خافت، ورفض واضح للملف التالف.
- التحقق من إتاحة المصادر الثلاثة لطبقة العرض بلا اتصال شبكة — المسألة ٨ في §١٨.

**الاعتماديات.** ٥.

**المخرجات.**
- الطبقات الثلاث والإعدادات وخدمة الخطوط.
- اختبارات: ملف تالف، خط بلا تغطية عربية، خط اختفى بعد اختياره، إعادة الإتاحة بعد الإقلاع.
- **Playwright:** تمركز السطر النشط في الآلة الكاتبة عند مواضع مختلفة، وتخفيت التركيز على النطاقات غير النشطة دون سواها، ومخرج Zen بلوحة المفاتيح، وسلوك تقليل الحركة.
- ADR للخطوط: الصيغ المدعومة وحد الحجم وطريقة الإتاحة.
- مصالحة §٨.

**معيار الاكتمال.**
- الطبقات الثلاث تعمل، ولكلٍّ تعطيل مستقل، والخروج بلوحة المفاتيح يعمل في كل الحالات.
- الآلة الكاتبة بلا اهتزاز على مستند ٢٠ ألف كلمة، وفورية عند تقليل الحركة.
- **التركيز لا يغيّر بايتًا واحدًا في الملف المحفوظ** — يُثبَت بمقارنة الملف قبل وبعد.
- تغيير الخط أو الحجم أو التباعد أو عرض العمود يظهر فورًا ويُحفظ ولا يمسّ المحتوى.
- خط بلا تغطية عربية يُقبل مع تحذير ويرجع للنطاقات العربية إلى Almarai.
- بطارية العربية مُعادة كاملة بعد دخول الخطوط والطبقات.

---

## المرحلة ٧ — الجودة والوصول والأداء · `quality`

**الهدف.** اجتياز بوابات الإصدار بمعايير مقاسة لا مفترضة.

**النطاق.**
- قائمة «تحقق قبل الإصدار» في `IMPLEMENTATION.md` §١٣ كاملة.
- اجتياز كامل بلوحة المفاتيح بترتيب تركيز منطقي في RTL، ومؤشر تركيز ظاهر لا يُحذف.
- VoiceOver بالعربية وبنص مختلط على المحرر والأسطح والحالات.
- تسمية وصفية عربية لكل زر أيقونة، وأهداف تفاعلية ٣٢×٣٢ فعليًا.
- تقليل الحركة يحوّل كل انتقال إلى فوري بلا فقد وظيفة، ويوقف نبض مؤشر الحفظ.
- تكبير نص النظام والواجهة بلا قصّ للأوامر الأساسية ولا كسر للمحرر.
- مراجعة التباين لكل رمز في الثيمات الخمسة، ولا معنى يُنقل باللون وحده.
- **تثبيت ميزانيات الأداء** من قياسات المراحل ١ و٣ و٦ — المسألة ١٠ في §١٨: زمن الفتح حتى مؤشر قابل للكتابة، زمن ظهور الحرف، زمن حفظ تعديل نموذجي، الذاكرة في جلسة ممتدة، زمن فتح مستند من مكتبة كبيرة، وحدود حجم المستند والمكتبة.
- مجموعة اختبارات §١٥ كاملة، ولقطات مرجعية للثيمات الخمسة.
- **Playwright:** اجتياز كامل بلوحة المفاتيح عبر كل السطوح، وفحص شجرة الوصول للتسميات والأدوار، وتأكيد ظهور حلقة التركيز على كل عنصر تفاعلي.
- **Lighthouse:** تدقيق وصول وأداء نهائي على طبقة الويب، ومقارنته بنتيجة المرحلة ٤ لكشف أي انحدار تراكمي.
- تشغيل بطارية العربية كاملة مرة أخيرة.
- اختبار حالات الخطأ لا الحالة المثالية: فشل الحفظ، لقطة تالفة، خط مفقود، قرص ممتلئ.

**الاعتماديات.** ٦.

**المخرجات.**
- تقرير جودة يغطي بنود §١٣ بندًا بندًا، يفصل ما تحقق آليًا عمّا تحقق يدويًا داخل التطبيق.
- جدول ميزانيات أداء مقاسة على جهاز macOS مستهدف **من داخل `Luma.app`** — لا من أرقام Lighthouse.
- قائمة تحقق يدوية قابلة لإعادة التشغيل قبل كل إصدار.
- مصالحة §١٣ و§١٤ و§١٥.

**معيار الاكتمال.**
- كل بنود §١٣ مجتازة.
- الميزانيات مثبتة **ومحققة**، لا مثبتة فقط.
- كل حالة خطأ مختبَرة وموثّقة بسلوكها الفعلي.

---

## المرحلة ٨ — التوقيع والتوزيع والإطلاق · `release`

**الهدف.** `Luma.app` موقّعة ومصدَّقة، تُثبَّت من DMG على جهاز نظيف بلا تحذير.

**النطاق.**
- **الحصول على عضوية برنامج مطوّري Apple وشهادة `Developer ID Application`** — أول خطوة فعلية في هذه المرحلة، لا مدخلًا يُنتظر قبلها.
- **استبدال معرّف الحزمة المؤقت `dev.luma.app` بالمعرّف النهائي** وتثبيته في كل مكان استُخدم فيه المؤقت (إعدادات الحزمة، الاستحقاقات، أي مرجع مباشر) — مرة واحدة، ولا يتغيّر بعدها.
- Hardened Runtime، وملف استحقاقات بالحد الأدنى الذي يحتاجه المنتج فعلًا.
- التوقيع، ثم التصديق عبر `notarytool`، ثم تثبيت التصديق على الحزمة.
- أيقونة التطبيق من مصدر ١٠٢٤ مربع غير مقصوص (صفحة Figma ٠٤)، بالمقاسات المطلوبة.
- حزمة DMG.
- قرار التحديث التلقائي: تفعيل أو تأجيل — صريح ومسجَّل، لا افتراض صامت.
- ترقيم الإصدار وسجل التغييرات.
- صفحة «حول»: الإصدار، ومكان حفظ النصوص، وبيان خصوصية مطابق لخط الأساس في §١١ — **بلا أي ادعاء عن موضع معالجة الصوت ما دامت المسألة مفتوحة**.
- اختبار تثبيت على حساب macOS نظيف.

**الاعتماديات.** ٧.

**المخرجات.**
- DMG موقّع ومصدَّق.
- تحقق ناجح من التوقيع والتصديق على جهاز نظيف.
- سجل تغييرات وقائمة تحقق إصدار.

**معيار الاكتمال.**
- التثبيت من DMG على حساب نظيف يفتح `Luma.app` بلا أي تحذير من Gatekeeper.
- دورة كاملة على ذلك الجهاز: كتابة، حفظ، إغلاق، إعادة فتح — مساحة نظيفة، والنصّ في المكتبة كما هو.

---

## متى تظهر `Luma.app`

في **المرحلة ١**، لأن الحكم على الغلاف لا يصحّ إلا داخل تطبيق مبني. أول نسخة صالحة للكتابة الفعلية في نهاية **المرحلة ٢**. النسخة الموقّعة القابلة للتوزيع في **المرحلة ٨** وحدها.

من المرحلة ١ فصاعدًا، لا مرحلة تُعلَن مكتملة قبل تجربتها داخل `.app` مبنية: الماوس ولوحة المفاتيح والمؤشر والتحديد والخطوط والثيمات والحفظ والإحساس العام. Playwright وLighthouse تُستخدمان عند فائدتهما وفق القاعدة الحاكمة أعلاه، **ولا تُعتبران بديلًا** عن ذلك.

---

## خارج نطاق هذه الخطة

مؤجَّل إلى ما بعد الإطلاق وفق `Luma.md` §١٨: مساحة الأفكار، الإملاء الصوتي، الأصوات الثلاثة، الواجهة الإنجليزية.

بنيتها موجودة في التصميم، ونقاط ارتباطها محجوزة في المعمارية، وبناؤها لاحقًا لا يغيّر ما سبقه. **لا يُبنى منها شيء قبل الإطلاق ولو بدا قريب المنال.**

---

## مدخلات مطلوبة

لا شيء يحجب بدء الخطة الآن.

- **معرّف الحزمة:** `dev.luma.app` مؤقتًا من المرحلة ١، والنهائي يُستخرج ويُثبَّت **داخل** المرحلة ٨ نفسها — ليس شرطًا مسبقًا لها.
- **Figma:** الملف كاملًا بعشرين صفحته متاح ومفهرَس أعلاه بمعرّفات العقد. لا حجب على أي مرحلة.
- **`Developer ID Application`:** الحصول عليها أول خطوة **داخل** المرحلة ٨، لا مدخلًا يُنتظر قبل بدئها أو قبل المراحل السابقة لها.
