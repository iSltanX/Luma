# خطة الإحساس — من تقرير فحص «لماذا لا تقول Luma واو»

مصدران لا ثالث لهما: **تقرير الفحص** (جلسة ٢٤ أغسطس ٢٠٢٦ — المراحل M0–M4، لا سبب لم يُشخَّص ولا اقتراح لم يرد فيه)، و**إضافات المالك** (٢٤ أغسطس مساءً — المراحل M5–M7، بفحص ميداني وثّق عيوبها قبل تدوينها).
الأسباب الثلاثة المشخَّصة في التقرير: **الزمن محذوف من التنفيذ كله** · **الضوء محذوف، في منتج اسمه يعني الضوء** · **لحظة الوعد تقع خلف الكواليس**.

## طريقة التشغيل

اكتب في المحادثة `m0` أو `m1` … `m4` فيعني: **ابدأ تنفيذ تلك المرحلة**. لا تُنفَّذ مرحلة دون هذا الأمر، ولا تُنفَّذ خارج ترتيبها إلا بطلب صريح.

| المرحلة | العنوان | الحالة |
|---|---|---|
| M0 | برهان التشخيص | **ثبتت** — حكم المالك: «جميل جدًا» (٢٤ أغسطس). أُصلح فيها انحدارٌ لاحق: ظل الورقة كان يتسرب إلى المحرر المريح |
| M1 | طبقة 300ms على كل الأسطح | **ثبتت** — حكم المالك: «نجح» (٢٤ أغسطس). وأثناء تجربتها ظهر **تجمّد عند استيراد خط**: عطل من المرحلة ٦ لا انحدار M1 (النواة لم تُمسّ في M0/M1)، أُصلح ومعه حارس يُسقط عودته |
| M2 | طبقة 500ms على التحول الهيكلي | منفَّذة — والسلاسة عولجت: الانسياب صار **يتتبّع التخطيط في كل إطار** بدل أن ينتظره أو يتنبّأ به |
| M3 | نظام الظلال كاملًا | منفَّذة |
| M4 | لحظة الحفظ الأول على المسرح | منفَّذة — `Luma.md` §٨ صار يستثني أول «محفوظ» في كل نصّ جديد، بتاريخه وسببه |
| M5 | شريط التنسيق أسفل مساحة الكتابة | منفَّذة — والخيارات في M11 |
| M6 | اتساق المحرر المريح | منفَّذة — والتركيز عولج في M10 |
| M7 | عنقود أفعال الشريط العلوي | منفَّذة — المداخل والأفعال عنقودٌ واحد بفاصل رفيع، والطرف المقابل فراغ خالص |
| **M8** | **سلامة النص عند حدود النواة** | **ثبتت** — حكم المالك على ⌘Q: «نعم أجدها» (٢٥ أغسطس) |
| M9 | ورقة الخط تنكسر بعائلة مكرَّرة | منفَّذة — العائلة اختيارٌ واحد لا وجه، والملفّان يبقيان مسجَّلين. حارسٌ أُثبت سقوطه |
| M10 | طبقة التركيز: لا مستطيل خلف السطر | منفَّذة |
| M11 | خيارات شريط التنسيق | منفَّذة — H3 · اقتباس كتلة · الوزن، عبر الطبقات كلها حتى القرص. و`Luma.md` §٥ حُدِّث **بعد** التنفيذ |

تُحدَّث الحالة عند كل تقدم: `لم تبدأ · جارية · ثبتت · موقوفة`.

> **درسٌ مدفوع الثمن (٢٥ أغسطس):** `npm run selftest` **لا يبني** — يشغّل ما في
> `target/release/bundle`، والوجه الويبي **مضمَّن في الثنائي** لا يُقرأ من `dist`.
> فكل «selftest 44/44» بين M2 وM5 كان يشغّل حزمةً بُنيت قبل M0، وأخفى انحدارًا
> حقيقيًا. **القاعدة: `npm run app:build` قبل `npm run selftest`، دائمًا.**
**كل المراحل منفَّذة.** ثبت بحكم المالك: M0 · M1 · M5 (الموضع) · M8. والباقي منفَّذ ينتظر **جولة الفحص الختامية**.

وثلاثة أعطال جاءت من التجربة لا من التقرير، وأُصلحت بحرّاسٍ أُثبت سقوطها: تجمّد استيراد الخط · اللصق يكدّس الأسطر · فقدُ الوزن صامتًا عند حدّ الجسر.

M8 تتصدّر لا لأنها إحساس، بل لأن «**سلامة النص أولًا**» تعلو كل خاصية ثانوية — والدفء فوق نصٍّ يضيع لا معنى له.

---

## M0 — برهان التشخيص، ولا شيء قبلها

- **التدخّل:** التغييرات المثبِتة الثلاثة كما نصّ عليها التقرير حرفيًا، معًا: (أ) «نفّذ سطر الجدول الأوسط وحده على سطح واحد — اللوحة الجانبية: دخول 300ms ease-out وخروج ease-in» في [EditorShell.svelte](src/components/EditorShell.svelte) (اللوحة عند `{#if panel…}` سطر 135)؛ (ب) «أضف قيم §٦ الثلاث كرموز وطبّق أولها على الورقة والمتوسط على شريط التحديد» — الرموز في [source.json](src/tokens/source.json) ثم `node src/tokens/generate.mjs`، والتطبيق على `.sheet` في [EditorShell.svelte](src/components/EditorShell.svelte) و[SelectionToolbar.svelte](src/components/SelectionToolbar.svelte)؛ (ج) «مرة واحدة لكل مستند جديد فقط: عند أول انتقال إلى saved، يظهر "محفوظ" بكامل حضوره ثم يخفت بعد لحظة إلى حالته الدائمة الخافتة» — شرط في [App.svelte](src/App.svelte) مستعملًا الـ`transition: opacity 200ms` القائم في [SaveStatus.svelte](src/components/SaveStatus.svelte) سطر 70.
- **السبب:** برهنة الأسباب الثلاثة المشخَّصة دفعة واحدة: «الزمن محذوف من التنفيذ كله»، «الضوء محذوف، في منتج اسمه يعني الضوء»، «لحظة الوعد تقع خلف الكواليس».
- **التحقّق:** تشغيل التطبيق داخل `Luma.app` (`npm run app`) لا على خادم الويب — لأن التقرير أسقط مشاهدات الويب: «خارج Tauri يعمل المحرر بلا تخزين إطلاقًا» — ثم قياس اللحظات الباردة الثلاث التي سمّاها بعينها: «افتح المكتبة وأغلقها» للأول، «انظر إلى الشاشة الساكنة قبل/بعد» وتحديد أول جملة للثاني، «اكتمال أول جملة في مستند جديد» بعد 700ms للثالث.
- **شرط التوقف:** **إن لم يتغيّر الإحساس في اللحظات الثلاث، فالتشخيص خاطئ والخطة كلها موقوفة حتى يُعاد الفحص.** وإن سقط برهان سبب واحد وثبت غيره، تُجمَّد مراحله وحدها ويُعاد فحصه، وتمضي مراحل ما ثبت — فالتقرير أعطى كل سبب شرط إكذاب مستقلًا: «إن لم يتغير الإحساس، فهذا التشخيص خاطئ».

**ملاحظة الترتيب:** ترتيب المراحل يطابق ترتيب الأسباب في التقرير. الخروج الوحيد أن السبب الأول قُسم مرحلتين (M1 ثم M2) لتبعية تقنية لا لسهولة: طبقة 500ms الهيكلية تُبنى فوق رموز الحركة ونمط الدخول/الخروج اللذين تثبتهما طبقة 300ms أولًا.

## M1 — السبب الأول: طبقة 300ms على كل الأسطح

- **التدخّل:** رموز حركة في [source.json](src/tokens/source.json) بقيم جدول التصميم كما نقلها التقرير (150ms تفاعلات، 300ms «فتح النوافذ المنبثقة واللوحات الجانبية»، 500ms هيكلية؛ «ease-out للدخول، ease-in للخروج») مع إعادة التوليد، وتحويل قيم M0 الحرفية إليها، ثم تعميم طبقة 300ms على بقية الأسطح المقيسة في التقرير: لوحة السجل، شاشة الإعدادات ([App.svelte](src/App.svelte) سطر 924)، ورقة الخط (سطر 938)، وشريط التحديد ظهورًا واختفاءً (سطر 953، [SelectionToolbar.svelte](src/components/SelectionToolbar.svelte)).
- **السبب:** السبب الأول «الزمن محذوف من التنفيذ كله (غائب — والتصميم نصّ عليه بالأرقام)»: «طبقتا 300ms و500ms اللتان خصّهما التصميم للأسطح والتحولات الهيكلية: **صفر**»، والقياس: «أول تحول DOM بعد ~36ms من النقرة، وكل أحداث transitionrun الملتقطة تخص لون زر الأيقونة نفسه لا السطح»، ولا رمز حركة واحد في `source.json`.
- **التحقّق:** بالعين داخل التطبيق: كل سطح من الخمسة يدخل منسابًا ويخرج منسحبًا بدل القطع الصلب؛ وتفعيل «تقليل الحركة» من الإعدادات يعيد كل شيء فوريًا كما قال التقرير إن البنية القائمة ([app.css](src/app.css) سطور 124–142) «ستحترمه تلقائيًا».
- **شرط التوقف:** أي حركة تمسّ النص أثناء الكتابة — فالتقرير أثبت أن «سكون النص أثناء الكتابة قرارٌ صحيح وموثق… الغائب حركة **الأسطح حول** النص» — أو أي انتقال لا يتحول فوريًا مع تقليل الحركة.

## M2 — السبب الأول: طبقة 500ms على التحول الهيكلي المسمّى

- **التدخّل:** دخول المحرر المريح وخروجه بطبقة 500ms (رمز M1) بدل التركيب الشرطي المتزامن: [App.svelte](src/App.svelte) سطور 229–254 (`enterComfort`/`exitComfort` مع `requestAnimationFrame(runTypewriter)` سطر 239) وتبديل التخطيط في [EditorShell.svelte](src/components/EditorShell.svelte) (`{#if !comfort}` سطر 94 وحشوة `.comfort .sheet`).
- **السبب:** لحظة السبب الأول المحددة في التقرير: «⌃⌘F إلى المحرر المريح — أكبر تحول هيكلي في المنتج، الذي خصّه التصميم بـ500ms "لتفادي الارتجاج البصري"، يقع قطعًا صلبًا: لقطة +100ms تُظهر المشهد النهائي مكتملًا».
- **التحقّق:** ⌃⌘F ثم Esc بالعين وبلقطة +100ms كما قاس التقرير: عند +100ms التحول في منتصفه لا مكتملًا، ويستقر نحو 500ms بلا ارتجاج؛ الخروج انسحاب ease-in؛ وتقليل الحركة يجعل الدخول والخروج فوريين.
- **شرط التوقف:** قفزة تخطيطية أو تعارض مع أول تمركز للآلة الكاتبة (الـrAF المذكور) أو أي أثر على سكون النطاق أثناء الكتابة — تعود المرحلة للمراجعة قبل المضي.

## M3 — السبب الثاني: نظام الظلال كاملًا

- **التدخّل:** استكمال خريطة §٦ الثلاثية التي نقلها التقرير بقيمها — خفيف `0 1px 2px rgba(0,0,0,0.04)` «للبطاقات والعناصر التفاعلية»، متوسط `0 4px 12px rgba(0,0,0,0.06)` «للقوائم والأدوات العائمة»، عميق `0 8px 24px rgba(0,0,0,0.08)` «للنوافذ الحوارية والإعدادات العائمة» — من الرموز المضافة في M0 إلى مواضعها المتبقية: ورقة الخط ([FontSheet.svelte](src/components/FontSheet.svelte))، شاشة الإعدادات وبطاقات الثيم ([SettingsScreen.svelte](src/components/SettingsScreen.svelte)، [ThemeCard.svelte](src/components/ThemeCard.svelte)).
- **السبب:** السبب الثاني «الضوء محذوف، في منتج اسمه يعني الضوء (غائب — والتصميم نصّ عليه بالقيم)»: «grep على box-shadow|gradient في src/ كلها يعيد ثلاث نتائج فقط، كلها حلقات تركيز inset — ولا رمز ظل في source.json»، واللحظة: «أداةٌ يفترض أنها "طفت" ولا شيء فيها يطفو».
- **التحقّق:** بالعين على الثيمات الخمسة: الأدوات العائمة تطفو، الحوارات ترتفع، والورقة منفصلة عن القماش بأكثر من فرق الدرجة اللونية — النظرة الساكنة التي وصفها التقرير «تدفأ».
- **شرط التوقف:** ظل يُحدث «فوضى بصرية» — القيد الذي نقله التقرير من §٦ نفسها — أو أي قيمة ظل مكتوبة يدويًا خارج الرموز (قاعدة «كل لون رمز ثيم»).

## M4 — السبب الثالث: لحظة الحفظ الأول على المسرح

- **التدخّل:** الوثيقة أولًا لأن التقرير حكم أن «المتهم بهذا القدر هو الوثيقة: قاعدة الصمت الدائم صحيحة، تعميمها على لحظة التكوين الأولى هو الخطأ» — تعديل [Luma.md](Luma.md) سطر 187 بقصر «شبه غير محسوسة» على ما بعد أول حفظ لمستند جديد، ثم تثبيت تغيير M0 (ج) تنفيذًا دائمًا في [App.svelte](src/App.svelte) و[SaveStatus.svelte](src/components/SaveStatus.svelte). (هذا هو الحد التنفيذي الكامل للسبب؛ ما وراءه موقف منتج لا كود، ولا تُخترع له مرحلة.)
- **السبب:** السبب الثالث «لحظة الوعد تقع خلف الكواليس (موجود ومسرحه خاطئ)»، واللحظة: «اكتمال أول جملة في مستند جديد — بعد 700ms تحدث ثلاث حقائق دفعة واحدة (مستند وُلد، حُفظ، دخل المكتبة) ولا يُرى منها شيء».
- **التحقّق:** داخل `Luma.app`: نص جديد، أول جملة، بعد نحو 700ms يظهر «محفوظ» بكامل حضوره ثم يخفت إلى حالته الدائمة؛ والحفظات التالية والمستندات القديمة تبقى خافتة كما كانت — مرة واحدة لكل مستند جديد لا غير.
- **شرط التوقف:** إن صار إشعارًا متكررًا أو خرق الثابت الذي استند إليه التقرير: «لا تتحول الحالة إلى شريط دائم يطالب بالانتباه» ([Luma.md](Luma.md) سطر 195).

---

## M5 — شريط التنسيق أسفل مساحة الكتابة *(إضافة المالك)*

- **التدخّل:** نقل شريط التحديد من الطفو فوق التحديد إلى موضع ثابت أسفل مساحة الكتابة، يظهر عند التحديد فقط ويختفي عند استئناف الكتابة — الثابت «لا شريط تنسيق دائم» ([Luma.md](Luma.md) §٥) محفوظ بحرفه — بأيقوناتنا وترتيبنا الحالي (عادي/H1/H2/«»)، ودخوله من الأسفل بطبقة 300ms ease-out وخروجه ease-in. الملفات: [App.svelte](src/App.svelte) (موضع `.floating` وشرطه)، [SelectionToolbar.svelte](src/components/SelectionToolbar.svelte)، [EditorShell.svelte](src/components/EditorShell.svelte) (منطقة الإرساء). داخل المحرر المريح يتقاسم المنطقة السفلية مع شريط الرقاقات (يعلوه أو يحل محله أثناء التحديد — يُحسم بالعين أثناء التنفيذ). ويُحدَّث تصميم العقدة 09 لاحقًا ليطابق — القرار قرار المالك وهو المصمم.
- **السبب:** طلب المالك (٢٤ أغسطس)، وترجيحي معه: المكان الثابت أقوى تحقيقًا لـ«نمط ثابت قابل للتعلم» ([Luma.md](Luma.md) سطر 128) من طفوٍ يتنقل مع كل تحديد، ولا يحجب سطرًا فوق التحديد، ويُنهي تلاصقًا مقيسًا اليوم بين الشريط العائم ونطاق الآلة الكاتبة في المحرر المريح (الشريط عند 326 والنطاق عند 372).
- **التحقّق:** بالعين في الوضعين: تحديد نص يُصعد الشريط من أسفل المساحة في مكانه الثابت دائمًا، أول حرف يُخفيه، ولا يسرق تركيزًا، وقارئ الشاشة يصله toolbar واحدًا بأسهمه كما اليوم.
- **شرط التوقف:** إن وجد المالك بُعد الشريط عن موضع التحديد يبطئ يده (مسافة الانتقال أطول من الطفو) تتوقف المرحلة ويُراجع القرار قبل حذف النمط العائم نهائيًا.

## M6 — اتساق المحرر المريح *(إضافة المالك — تُنفَّذ قبل M2)*

- **التدخّل:** إصلاح العيوب الموثقة بفحص ٢٤ أغسطس الميداني: **(١)** التركيز بالجملة يخفت الجملةَ النشطة نفسها حين يقف المؤشر بعد علامة نهاية جملة — قِيس: الفقرة النشطة مخفتة 51/51 محرفًا ولا محرف واحد بحبر كامل، فتصير الشاشة كلها خافتة بلا شيء «في التركيز» — الإصلاح في `sentenceAt`/`focusModePlugin` ([EditorCore.ts](src/editor/EditorCore.ts))؛ **(٢)** الخروج يقفز بالسطر النشط نحو 250px (قِيس: 384←133، وفي المستند القصير يصفّر التمرير) لأن تغيّر حشوة الورقة لا يُعوَّض في `scrollTop` — الإصلاح في `exitComfort` ([App.svelte](src/App.svelte))، وهو شرط «بلا قفزة تخطيطية» ([IMPLEMENTATION.md](IMPLEMENTATION.md) سطر 531) الذي يقيس عليه M2 حركته لاحقًا؛ **(٣)** تفعيل رقاقة «الآلة الكاتبة» يُظهر النطاق ولا يعيد التمركز حتى أول ضغطة — استدعاء `runTypewriter` عند تفعيل الطبقة؛ **(٤)** لا معالجة لتغيير حجم النافذة في أي طبقة (grep: لا مستمع resize) — إعادة تمركز عند التحجيم داخل المحرر المريح؛ **(٥–)** جرد مراجعة حارس الواجهة في الملحق أدناه، وعلى رأسه عيبان عاليان: ⌃⌘F من داخل الإعدادات يفتح محررًا مريحًا مقفولًا (تحقق سلوكي: `contenteditable="false"` والكتابة لا تصل)، وشريط النطاق شبه غير مرئي في الثيمات الخمسة (1.03:1).
- **السبب:** طلب المالك: «به المزيد من الخطأ وغير متسق» — والقياسات أعلاه دليله الموثق.
- **التحقّق:** بالعين داخل `Luma.app`: الوقوف بعد نقطةٍ يُبقي جملتها بحبر كامل وما حولها خافتًا؛ الخروج لا يقفز بالسطر النشط؛ تفعيل الرقاقة يحضر السطر إلى النطاق فورًا؛ تحجيم النافذة يعيد التمركز؛ وبند `comfort-focus` في selftest يبقى أخضر.
- **شرط التوقف:** أي إصلاح يلمس بايتًا من المحتوى (التركيز عرضٌ بحت — بند selftest «لا يغيّر بايتًا») أو يكسر ثابت §٧ «الخروج لا يغيّر موضع المؤشر ولا حالة النص».

### ملحق M6 — جرد العيوب (فحص ميداني + مراجعة حارس الواجهة، ٢٤ أغسطس)

**عالٍ:**
- ⌃⌘F والإعدادات مفتوحة → محرر مريح مقفول: `enterComfort` لا يعيد `setEditable(true)` ويستدعي `focus()` قبل `tick()` والإطار `inert`، ويسرّب `fontSheet` بلا تصفير — [App.svelte](src/App.svelte) سطور 473–477 و255–266 مقابل 519–521 و554. **تحقق سلوكيًا.**
- شريط النطاق غير مُدرَك في الثيمات الخمسة: `surface-paper` على `surface-canvas` ≈ 1.03–1.07:1 — [EditorShell.svelte](src/components/EditorShell.svelte) سطر 401، والملف نفسه يقرّ بالمشكلة للورقة ويحلها بحدّ (سطور 317–319) ويحرم الشريط منه.

**متوسط:**
- التركيز بالجملة يخفت الجملة النشطة نفسها بعد علامة الترقيم (قِيس 51/51) — [EditorCore.ts](src/editor/EditorCore.ts) `sentenceAt`/`focusModePlugin`.
- قفزة الخروج ~250px بلا تعويض تمرير (قِيس 384←133؛ والقصير يصفّر) — `exitComfort` [App.svelte](src/App.svelte) سطور 274–280.
- مرجعان للارتفاع: حشوة `45.5vh` من النافذة وشريط `45.5%` من `.writing` ومرساة تقيس `.scroller` — فارق ≈22px يترك سطر بداية المستند خارج شريطه كاملًا؛ و`comfortPadding()` الموحِّدة موجودة مهجورة — [EditorShell.svelte](src/components/EditorShell.svelte) 386 و397، [typewriter.ts](src/lib/typewriter.ts) 21 و97–102.
- شريط التحديد معفى من منظومة المحرر المريح: لا يعرف `comfort` ولا `zenHidden`، وz-index يتعادل مع زر الخروج والغلبة لترتيب DOM — [App.svelte](src/App.svelte) 984–1002 و1013. *(تلتقيها M5 جذريًا بنقله أسفل المساحة.)*
- فتح الإعدادات يُسقط comfort نهائيًا عند الإغلاق ويسرّب `zenHidden` قديمًا — `openSettings`/`closeSettings` [App.svelte](src/App.svelte) 523–530.
- رقاقة «الآلة الكاتبة» لا تمركز (قِيس)، والدخول والطبقة مطفأة يزيح ~45vh بلا تصحيح — `syncLayers` 165–168 و`enterComfort` 265.

**منخفض:** زر الخروج وشارة الفشل يقطعان شريط السحب (`--space-024` في شريط 48px)؛ Zen يخفي بالشفافية ويُبقي النقر فاعلًا؛ مساران بصريان للفشل داخل comfort (`fail` + `notice`)؛ العناوين أطول من شريط النطاق؛ `notice` يفكّ تطابق الشريط والمرساة؛ `.comfort .scroller` في كتلتين؛ الرقاقة المفعَّلة بلا hover؛ ترتيب الطبقات معكوس بين الشريط والإعدادات؛ لا تمركز عند resize (قِيس)؛ `zenHidden` لا يُصفَّر عند إعادة تفعيل Zen؛ رمز `--text-editor-comfort` (١٩/٤٠) مولَّد لا يستهلكه أحد — تباعد «المريح» المرسوم لا يُطبَّق قط؛ `ComfortButton` مكوّن ميت بوثائق مضللة.

**سلم من الفحص:** تباين طبقة التخفيت في الثيمات الخمسة (5.1–5.8:1) وسلوك التحديد فوقها؛ Zen recede/return؛ سكون التمرير أثناء الكتابة داخل النطاق (تغييران فقط في جملة كاملة).

## M7 — عنقود أفعال الشريط العلوي *(إضافة المالك)*

- **التدخّل:** توحيد أيقونات الشريط العلوي في عنقود واحد عند بداية القراءة: [المكتبة، السجل الزمني | فاصل رفيع | نصّ جديد، المحرر المريح] وترك الطرف المقابل فراغًا خالصًا — [SurfacesBar.svelte](src/components/SurfacesBar.svelte) وحده.
- **السبب:** طلب المالك المشروط برأيي، ورأيي معه: الفعلان اليوم يتيمان في الطرف البعيد (جزيرتان متباعدتان تشتتان العين)، وعنقود واحد عند بداية القراءة أقصر لعين RTL وأهدأ للشريط، والفاصل الرفيع يمنع خلط «مدخل لوحة» بـ«فعل».
- **التحقّق:** بالعين: شريط أهدأ بلا لبس بين المداخل والفعلين، وترتيب Tab منطقيًا (اللوحات ثم الفعلان)، وبند `window-controls` في selftest يبقى أخضر.
- **شرط التوقف:** حكم المالك — إن قتل التجميعُ توازنَ الشريط الحالي يُرتد عن المرحلة فورًا.

---

## M8 — سلامة النص عند حدود النواة *(مصدرها ثالث: فحص البقعة العمياء، ٢٤ أغسطس)*

- **التدخّل:** أربعة مسارات تفقد نصًّا، كلها على حدّ النواة/الواجهة: **(١)** بند «إنهاء Luma» يصير عنصر قائمة بمعرّف وباختصار ⌘Q بدل `PredefinedMenuItem::quit` — فيمرّ بـ`on_menu_event` ثم بـ`flush-and-close` ثم `app.exit(0)`، لأن `RunEvent::ExitRequested` **لا يقع أصلًا** على مسار `terminate:` ([lib.rs:387](src-tauri/src/lib.rs#L387))؛ **(٢)** مزلاجا `CLOSING`/`EXITING` ([lib.rs:282](src-tauri/src/lib.rs#L282) و[291](src-tauri/src/lib.rs#L291)) لا يُستهلكان قبل أن تُعلن الواجهة جاهزيتها، ويُفتحان بعد كل دورة إفراغ لا عند الفشل وحده، مع سقف زمني يمنع حبس المستخدم؛ **(٣)** الجلسة تُنشأ قبل الخطوط والتفضيلات، أو يُحتفظ بما كُتب قبلها ليُعاد إليها — فما يُكتب في نافذة الإقلاع اليوم لا تعرفه `EditorSession` أبدًا ([App.svelte:618–619](src/App.svelte#L618) مقابل إنشائها بعد [694](src/App.svelte#L694))؛ **(٤)** مستمع `luma://menu` يتجاهل `undo`/`redo` حين تعلو شاشةٌ المحرر أو تجري معاينة ([App.svelte:716](src/App.svelte#L716)).
- **السبب:** ليس من التقرير — من فحص البقعة العمياء الذي أطلقه تجمّد استيراد الخط. أربعة أعطال ثبتت بعد تفنيد مستقل، ضررها «فقد نص»، وكلها خارج ما تبلغه الفحوص الثلاثة (e2e بلا نواة، و`selftest` لا يغلق نافذة قط، و`cargo test` لا يشغّل حلقة التطبيق).
- **التحقّق:** داخل `Luma.app` لكل مسار: اكتب جملة ثم ⌘Q فورًا — تعود عند الفتح. اضغط ⌘Q في أول نصف ثانية من الإقلاع ثم اكتب وأغلق — يُحفظ. الصق فقرة فور ظهور المؤشر ثم افتح مستندًا آخر — لا تضيع. افتح الإعدادات واضغط ⌘Z — لا يتغيّر النص تحتها. ويسبق كلَّ إصلاح إثباتٌ للعطل بأداة قياس مؤقتة، فلا يُصلَح ما لم يُرَ.
- **شرط التوقف:** أي إصلاح يجعل الإغلاق قابلًا للتعليق بلا مخرج (المزلاج وُضع أصلًا لئلا يعلق المستخدم إن فشل الحفظ)، أو يؤخّر ظهور المؤشر عن سقف الإقلاع في [ADR ٠٠١١](docs/decisions/0011-performance-budgets.md).

## M9 — ورقة الخط تنكسر بعائلة مكرَّرة

- **التدخّل:** مفتاح صفوف الخطوط في [FontSheet.svelte:191](src/components/FontSheet.svelte#L191) يصير فريدًا بالمسار لا بالاسم، أو تُدمَج أوجه العائلة الواحدة في مدخل واحد عند التعداد في [fonts.rs:283](src-tauri/src/fonts.rs#L283).
- **السبب:** فحص البقعة العمياء: استيراد وجهين من عائلة واحدة (Regular ثم Bold، أو الملف نفسه مرتين باسم `‑1`) ينتج مفتاحًا مكرَّرًا في `{#each}` فتنكسر الورقة ولا مخرج منها داخل التطبيق — «حالة عالقة».
- **التحقّق:** استورد وجهين من عائلة واحدة: يظهران بلا كسر، والاختيار والإغلاق يعملان.
- **شرط التوقف:** أن يفقد المستخدم القدرة على تمييز وجهين من عائلة واحدة، أو أن يتغيّر خط مستند محفوظ نتيجة الدمج.

---

## ما كشفه جرد M11 ولم يدخله — بترتيب الضرر

- **اللقطات بلا بوابة إصدار:** `revision.rs:115` و`:93` يقرآن اللقطة بـ`from_slice` مباشرةً بلا فحص `schemaVersion` (الحقل موجود ولا يُقرأ) وبلا هجرة — عكس قاعدة المستندات في `document.rs:176` تمامًا. ولقطةٌ من إصدار أحدث تُقرأ بالتخمين لا تُرفض. عطلٌ قديم لا يخصّ M11.
- **الذاكرة الزمنية عمياء عن التنسيق:** `revision.rs:47` يقرّر اللقطة بفارق **عدد الحروف** (٨٠ محرفًا)، وتحويلُ فقرة إلى عنوان أو تغميقُها يغيّر **صفر حرف** — فلا لقطة قبل تغيير تنسيقي كاسح، ولا شبكة أمان للتراجع عنه بعد الإغلاق. صار هذا ذا معنى فعلي بعد M11.
- **العنوان والمقتطف لا يعرفان الدور:** `model.rs:48` يجعل أول كتلة غير فارغة عنوانَ المستند — فاقتباسٌ مفتتِح يصير عنوانًا في المكتبة وشريط النافذة. و`:142` يجمع النصوص بمسافة واحدة متجاهلًا الدور.
- **الفقد الصامت عند حدّ الجسر:** لا `deny_unknown_fields` في أي بنية بالنواة، فالحقل الذي لا تعرفه Rust يُبتلع بلا شكوى — وهو بعينه ما جعل غياب `marks` عطلًا صامتًا لا خطأ بناء.

## خارج الخطة

أشياء وردت في التقرير دليلًا لا اقتراحًا، أو لم ترد فيه أصلًا — لا تدخل المراحل، وتُطرح للنقاش مستقلةً:

- **المؤشر بلون النحاس:** التقرير استشهد بأن «المؤشر الوامض يتبع الحبر لا النحاس» ([editor.css](src/editor.css) سطر 50) دليلًا على غياب «النواة النحاسية»، لكنه لم يقترح تغييره — وتغييره يمس قرارًا موثقًا في الكود («المؤشر يتبع حبر النص لا لون النظام»).
- **Zen مطفأ افتراضيًا:** ذكره التقرير دليلًا (الخفوت الوحيد في المنتج معطَّل ابتداءً — [preferences.svelte.ts](src/lib/preferences.svelte.ts) سطر 55) دون اقتراح قلب الافتراضي؛ قلبه قرار منتج له ثوابته في `Luma.md`.
- **توثيق القيم المنقولة:** نقل مُدد §١١ وظلال §٦ من Figma إلى الرموز قرارٌ يمس نظام الرموز المولَّد، وعُرف المشروع تدوين ما حُسم في `docs/decisions/` — ADR قصير بعد ثبوت M0، وتحديث [IMPLEMENTATION.md](IMPLEMENTATION.md) سطر 505 الذي صار وصفه («يتحوّل كل انتقال إلى تغيّر فوري») صادقًا فعلًا بعد وجود انتقالات تُعطَّل.
- **المتبقّي بعد M8 — إنهاءٌ لا يمرّ بالقائمة:** بند «إنهاء Luma» صار بمعرّف فيمرّ بالحفظ، لكن `terminate:` يصل أيضًا من **قائمة Dock** ومن **حدث Apple** ومن تسجيل الخروج، وهذه لا تمرّ بقائمة التطبيق فيصلها `RunEvent::Exit` — ذراعٌ لا تُمنع (قِيس بعد الإصلاح: بقيت كما هي). ويخفّفها أن فقد التركيز محفّز كتابة فورية، فالتطبيق الذي تُنهيه من Dock غالبًا أفرغ ما لديه. وسدُّها الكامل يقتضي نسخةً ظلّية في النواة تُكتب عند `Exit` — قرار بنية لا إصلاح.
- **حارس يمنع عودة المُدد الحرفية:** بعد M1 صارت كل مدّة انتقال رمزًا، ولا شيء يمنع كتابة `200ms` جديدة غدًا — حارسٌ على غرار «لا لون مكتوب يدويًا» يجعل النسيان خطأ بناء. لم يرد في التقرير فلم يدخل مرحلة.
- **الدوّارتان (٨٠٠ms و٩٠٠ms):** بقيتا حرفيتين في [Button.svelte](src/components/Button.svelte) و[SaveStatus.svelte](src/components/SaveStatus.svelte) — مُدد حلقة لا انتقال، وجدول §١١ يصف الدخول والخروج والتحولات ولا يصف إيقاع الدوران. تحتاج قرارًا لا تنفيذًا.
- **تصميم العقدة ٠٩ لم يُحدَّث بعد:** الشريط في Figma ما زال طافيًا فوق التحديد، والكود صار يرسيه أسفل المساحة بقرارك. المطابقة قرارُ تصميمٍ لا كود، ولك وحدك.
- **ظلٌّ يعرف الثيم:** القيم الثلاث واحدة في الثيمات الخمسة كما رسمها التصميم، وفي «ليل» أسودُ ٤٪ على `#13161c` لا يكاد يُدرَك — والفصل هناك يقع على الحدود وفرق السطح كما ينصّ §٩ نفسه. جعلُ الظل رمزًا يتبع الثيم قرارُ توسعةٍ لطبقة الرموز، لا إصلاحٌ.
- **«الضوء الطبيعي يملأ المكان» بما يتجاوز الظل:** الحالة العاطفية في عقدة 08 أوسع من نظام الظلال (مصدر ضوء، تدرج، مادة)، والتقرير لم يقترح لها تدخلًا — إن ثبتت M0 وM3 وبقي السطح باردًا، فهذا موضوع فحص جديد لا امتداد لهذه الخطة.
