# مراجعة فحص المواضع — الثوابت والعربية/RTL

مراجعة لـ[POSITIONS-AUDIT.md](POSITIONS-AUDIT.md) على سؤالين: هل تمسّ أيٌّ من
ملاحظاته بندًا موسومًا **ثابت**؟ وأين عوملت العربية طبقةً مضافة لا أصلًا؟

تاريخ المراجعة: ٢٠٢٦-٠٨-٢٥

---

## السؤال الأول: ما يمسّ ثابتًا

**الملاحظة ١ — عدّاد الكلمات يُربط بحافة الورقة:** لا تمسّ ثابتًا. فحصت
البنود الأقرب واحدًا واحدًا: §٥ في Luma.md («مخفي افتراضيًا، يظهر بطلب
المستخدم، ولا يرتبط بهدف» **ثابت**) يقيّد السلوك لا الموضع، والتنفيذ لا يغيّر
السلوك. §١٠ في IMPLEMENTATION.md («لا يتغيّر المستند ولا الورقة ولا شريط
النافذة ولا موضع المؤشر» **ثابت**) لا يذكر العدّاد بين المحميات — والعدّاد
اليوم أصلًا يتحرك عند فتح لوحة لأنه مربوط بحافة `.writing` المتحركة، فالتنفيذ
يقلّل حركته لا يستحدثها. و§١٦ («بداية القراءة يمين») يتقوّى بالتنفيذ لأنه
يقرّب العدّاد من بداية قراءة النص نفسه. والرقاقة بلا مقابل في الصفحة ١٧ أصلًا
(ADR ٠٠٠٨ ركّبها من `Badge`)، فلا موضع مرسوم يُخالَف. **تقع في مفتوح.**

**الملاحظة ٢ — نصف قطر شريط التحديد ١٢→٨:** لا تمسّ ثابتًا. §٥ («تظهر عند
تحديد النص وتختفي... بنمط ثابت» **ثابت**) عن الظهور والموضع لا عن الهندسة،
ولا يمسّهما التنفيذ. جدول §١٢ «مقاسات ثابتة من التصميم» يثبّت **سلّم** أنصاف
الأقطار (٤·٨·١٢·١٦·٢٤·كامل) لا إسناد قيمة بعينها لمكوّن بعينه — و٨ خطوة في
السلّم، والانتقال من رمز إلى رمز لا يخالف قاعدة الرموز. يبقى وجه واحد: مخالفة
رسم Figma للعقدة `113:375`، وسابقتها موثّقة في §١٢ نفسه («حيث يتعارض التصميم
مع الوثيقتين، الوثيقتان أعلى» — حالة الظل)، لكن هنا لا وثيقة توجب التغيير بل
قياس. **تقع في مفتوح**، بشرط مزامنة Figma بعد التنفيذ وإلا صار ملف التصميم
يكذب — وهو ما تحذّر منه ديباجة §١٢.

**الملاحظة ٣ — لوحة تُبلَغ بـTab واحدة:** لا تمسّ ثابتًا، لكنها الوحيدة التي
تحتكّ بنصّين فانتبه للآلية:

- الغاية نفسها **هي ما يصفه §١٠ حرفيًا**: «اللوحة أول الجسم في الشجرة... وهي
  التالية لمدخلها في ترتيب Tab» — فقرة غير موسومة (وصف حالة قائمة)، والواقع
  المقيس يكذّبها. التنفيذ يُعيد الكود إلى وصف الوثيقة لا يخالفه.
- الاحتكاك الأول: §١٠ يقول بخط عريض «**الفتح لا ينقل التركيز**». آلية تنقل
  التركيز إلى اللوحة عند فتحها تُبطل هذا المقصد (كاتب فتح المكتبة بالفأرة
  يفقد مؤشره من النص). الملاحظة كما كُتبت لا تقترح ذلك، وأيّ تنفيذ يجب ألّا
  ينزلق إليه.
- الاحتكاك الثاني: §١٣ («ترتيب التركيز منطقي في RTL» — **توجه** كمعيار،
  **ثابت** كاتجاه). من خيارَي الملاحظة، خيار نقل اللوحة قبل شريط الأسطح في
  DOM يجعل Tab يقفز من شريط العنوان إلى لوحةٍ تحت الشريط بصريًا ثم يعود
  فوقها — كسرٌ لمنطقية الترتيب أسوأ من الداء. **أرجّح** الخيار الأول (المدخل
  النشط آخر محطات الشريط، فتصل Tab التالية إلى اللوحة) لأنه يحفظ الاتجاه
  الثابت في §١٣ ويحقّق وصف §١٠ معًا.

---

## السؤال الثاني: العربية والانعكاس

شغّلت الواجهة (وهي RTL دائمًا بتصريح `dir=rtl` — والفارق بين النظامين في
chrome النافذة وحده، وذاك حرم ADR ٠٠٠٣). قست كل عنصر ذي اتجاه: المنزلقات
الثلاثة (الحد الأقصى يسارًا والامتلاء من اليمين — قست مقبض ٨٠٠/الأقصى عند
الطرف الأيسر تمامًا، و١٩ من [١٤–٢٦] عند ٤١٪ من اليمين بدقة، وسهم اليسار
**يزيد** القيمة فالمفاتيح توافق العين)، وحقل البحث (الأيقونة عند بداية القراءة
والمسح عند نهايتها، منطقيًّا لا بالجهات)، وسهما `NavRow` و`link-go` (مقلوبان
إلى اليسار — اتجاه التقدّم الصحيح)، وترتيب أدوات شريط التحديد ورقاقات الوضع
المريح وبطاقات الثيم (يبدأ كلها بالافتراضي من اليمين)، وأزرار تذييل ورقة الخط
(الأول يسارًا — مرآة عرف macOS الصحيحة). كلها عربية أصيلة. **وجدت موضعًا
واحدًا، فأقول واحدًا:**

### المفتاح (Toggle) يتحرك حركة غربية غير معكوسة — عكس وثيقته وعكس تعليقه هو

**ما هو الآن:** قست في التطبيق مشتغلًا على مفتاح «عدّاد الكلمات»: مطفأً
المقبضُ ملاصق للطرف **الأيسر** (فجوة ٢px يسارًا، ٢٢px يمينًا)، وبالتشغيل يقفز
إلى الطرف **الأيمن** (٢px يمينًا). وLuma.md §١٦ — والقسم يفتتح بـ«الواجهة RTL
أصيلة لا معكوسة» **ثابت** — ينصّ حرفيًا: «التشغيل يحرّك المقبض **يسارًا**».
وتعليق المكوّن نفسه في [Toggle.svelte:6](src/components/Toggle.svelte:6) و
[Toggle.svelte:51](src/components/Toggle.svelte:51) يدّعي «يبدأ يمينًا
(إيقاف) ويتحرك يسارًا (تشغيل)» — والكود يفعل العكس تمامًا. السبب في
[Toggle.svelte:52](src/components/Toggle.svelte:52) و
[Toggle.svelte:58](src/components/Toggle.svelte:58): `margin-inline-start: auto`
يدفع المقبض نحو **نهاية** السطر (اليسار في RTL) لا بدايته — منطق الهامش
التلقائي مقلوب في ذهن كاتبه.

**لماذا هو «طبقة مضافة» لا خطأ قيمة:** المفتاح بوضعه الحالي هو مفتاح إنجليزي
لم يُمرَّر على المرآة (تشغيل = يمين، كما في نظام إنجليزي حرفيًا)، بينما
المنزلق الذي **يجاوره في العمود نفسه** معكوس صحيحًا — فعنصرا تحكم في شاشة
واحدة يجريان في اتجاهين منطقيين متضادين، ومن اعتاد مفاتيح macOS العربية
(تشغيل = يسار في «إعدادات النظام» كلها) يقرأ مفاتيح Luma الثلاثة المفعَّلة
افتراضيًا (الآلة الكاتبة والتركيز) مطفأة.

**الصواب:** تبديل الجهتين المنطقيتين في القاعدتين: القاعدة الأساسية
`margin-inline-end: auto` (إيقاف = المقبض عند بداية القراءة، اليمين)، وحالة
`checked` تعكسها (تشغيل = يسار). سطران، ويصير التعليق القائم صادقًا بلا
تعديل.

**التحقّق:** `npm run dev` → `?settings` → «الكتابة» → بدّل «عدّاد الكلمات»:
المقبض يجب أن يسكن يمينًا مطفأً ويقفز يسارًا مشتغلًا — طابِقه مع أي مفتاح في
«إعدادات النظام» على نظام عربي.

---

## تصحيح ذاتي

تقرير [POSITIONS-AUDIT.md](POSITIONS-AUDIT.md) وسم بند «كشف Zen عبر التركيز»
سليمًا، وهو سليم سلوكًا، لكن رقاقات Zen هذه مبنية على `ToggleChip` لا على
`Toggle`، فلا يطالها خلل المفتاح أعلاه — الخلل محصور في مفاتيح شاشة الإعدادات
الخمسة (`Toggle.svelte`).
