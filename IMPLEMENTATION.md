# Luma — أساس التنفيذ على macOS

أرضية عملية لتحويل Luma إلى تطبيق macOS أصيل. تعريف المنتج ونطاقه وسلوكه في [Luma.md](Luma.md)، ولا يُعاد هنا.

الأوسمة كما في `Luma.md`: **ثابت** التزام لا يُساوَم عليه · **توجه** اقتراح عملي قابل للاستبدال · **مفتوح** يحتاج قياسًا أو قرارًا.

---

## 1. المنصة والتقنية

| البند | القرار | الحالة |
|---|---|---|
| المنصة | macOS فقط في هذه المرحلة | **توجه** (حد مرحلة) |
| الحد الأدنى للنظام | macOS 13.0 مبدئيًا — يُراجَع في مرحلة الجودة | **توجه** |
| الغلاف | Tauri — نواة أصلية مجمَّعة، ونافذة عرض WKWebView | **توجه** — اجتاز بوابة القدرات ([ADR ٠٠٠١](docs/decisions/0001-shell-tauri.md)) |
| الواجهة ونواة المحرر | طبقة ويب داخل نافذة العرض | **توجه** — تابع لقرار الغلاف |
| أساس نواة المحرر | ProseMirror | **توجه قوي** — بقياس ([ADR ٠٠٠٢](docs/decisions/0002-editor-core.md)) |
| البناء والتوقيع والتوزيع | أدوات Apple عند الحاجة | **توجه** |

الاتجاه الحالي هو Tauri: تطبيق macOS أصيل يقبل التوقيع والتصديق والتوزيع بأدوات Apple، ومنطق التخزين والحفظ في نواة مجمَّعة مستقلة عن الواجهة.

هذا **توجه لا ثابت.** اختُبرت القدرات المطلوبة أدناه داخل `Luma.app` مبنية واجتازها الغلاف كلها، فثُبِّت — ويبقى عجزٌ لاحق عن واحدة من قدرات «ما يجب أن يأتي من المنصة» سببًا كافيًا لإعادة فتحه.

### ما يجب أن تحققه نواة المحرر

المطلوب سلوك لا تقنية. أي أساس يُختار لنواة المحرر يجب أن يوفّر هذه كلها، ويسقط إن عجز عن واحدة:

- **تحكم في التمرير بحسب موضع المؤشر** — وضع الآلة الكاتبة يحتاج معرفة مستطيل المؤشر بعد كل إدخال، وتمرير مساحة النص ليستقر السطر النشط عند نطاق مريح، وحشوة علوية وسفلية تكفي ليصل السطر الأول والأخير إلى ذلك النطاق.
- **طبقة عرض تخفت النص المحيط من دون لمس النموذج** — وضع التركيز سمات عرض مؤقتة، لا تعديل على المحتوى المخزَّن ولا على بنيته.
- **صحة العربية** — موضع المؤشر عند نهاية السطر، امتداد التحديد، الاتجاه الأساسي للفقرة، والنص المختلط قابلًا للتحرير.
- **تجميع التراجع بحسب دفقة الكتابة** لا بحسب الحرف الواحد.
- **تخطيط لا يعيد رسم المستند كله مع كل حرف**، ويبقى مقبولًا على مستند عربي طويل.

### ما يجب أن يأتي من المنصة لا أن يُبنى يدويًا **ثابت**

قوائم النظام السياقية للنص · الإملاء الأصلي في macOS · خدمات النص · اختصارات التحرير القياسية (تراجع، إعادة، قص، نسخ، لصق، تحديد الكل) · دعم VoiceOver للنص.

إعادة بناء أيٍّ من هذه يدويًا مؤشرٌ على أن الأساس المختار خاطئ، لا دعوةٌ إلى كتابة بديل.

**حُسم:** الأساس هو ProseMirror. مخططه يطابق `Block` واحدًا لواحد، و`Decoration` طبقة عرض لا تمسّ `state.doc`، و`coordsAtPos` يعطي مستطيل المؤشر متزامنًا، و`newGroupDelay` يجعل تجميع التراجع قرار منتج. سقط `contenteditable` الخام بعجزه عن القدرتين ٢ و٤، وسقط Lexical بعجزه عن القدرة ١. التفصيل والأرقام في [ADR ٠٠٠٢](docs/decisions/0002-editor-core.md).

---

## 2. البنية العامة

طبقات بحدود واضحة، والاتجاه من الأعلى إلى الأسفل فقط:

```text
┌─ Presentation ── واجهة التطبيق · EditorCore
├─ Session ─────── EditorSession · AppState · PanelState
├─ Domain ──────── Document · Block · Revision · Idea · Preferences · Theme · FontRef
├─ Services ────── Autosave · RevisionStore · LibraryIndex · FontService
│                  ThemeService · AudioService · DictationService
└─ Storage ─────── DocumentStore · PreferencesStore · FileSystem
```

الطبقات حدود مسؤولية لا حدود لغة. في الغلاف الحالي تقع Storage وما يلزمها من Services في النواة الأصلية، وتقع Presentation وSession في طبقة العرض — وهذا توزيع **توجه** تابع للقسم ١، أما الحدود نفسها فثابتة.

**قواعد المعمارية** **ثابت**

- المحرر نواة مستقلة لا تعرف شيئًا عن المكتبة ولا الصوت ولا الإملاء ولا الإعدادات.
- المحتوى منفصل عن تفضيلات العرض. تغيير خط أو ثيم لا يمسّ `Document`.
- المكتبة **فهرس فوق** المستندات، لا مالكة للمحتوى.
- السجل الزمني منفصل عن واجهة التحرير ويتغذى من الحفظ.
- فشل أي خدمة ثانوية لا يمنع الكتابة ولا الحفظ ولا الاستعادة. كل خدمة تفشل بمعزل وتُبلغ بصمت مقبول.

### الوحدات ومسؤولياتها

| الوحدة | المسؤولية | لا تفعل |
|---|---|---|
| `EditorCore` | عرض النص، الإدخال، المؤشر، التحديد، التراجع، طبقات الآلة الكاتبة والتركيز | لا تحفظ ولا تعرف المكتبة |
| `EditorSession` | حالة مستند مفتوح واحد: النموذج، التحديد، الأوضاع، ربط الحفظ | لا تلمس الملفات مباشرة |
| `AppState` | المستند الجاري، التفضيلات، حالة اللوحات، طبقات المحرر المريح | لا تحتوي منطق تخزين |
| `AutosaveService` | تجميع التغييرات وجدولة الكتابة وإدارة الفشل وإعادة المحاولة | لا تحجب الإدخال |
| `DocumentStore` | قراءة وكتابة ذرّية للمستندات | لا تعرف الواجهة |
| `RevisionStore` | إنشاء اللقطات وقراءتها وتطبيق الاستعادة | لا تحذف الحالة الحالية |
| `LibraryIndex` | فهرس مشتق للعرض والترتيب والبحث | ليست مصدر حقيقة |
| `FontService` | تعداد الخطوط، الاستيراد، التسجيل، فحص التغطية، الرجوع الآمن | لا تُخفي خطوطًا تلقائيًا |
| `ThemeService` | تحميل الثيمات وتطبيق الرموز | لا تحتوي ألوانًا موزعة في الواجهة |
| `AudioService` | تشغيل الأصوات الثلاثة والمستوى والإيقاف الشامل | لا تبدأ تلقائيًا |
| `DictationService` | الصلاحية، الالتقاط، النتائج الجزئية والنهائية، الأخطاء | لا تحتفظ بصوت |
| `IdeasStore` | حفظ مواد مساحة الأفكار | لا تصنّف ولا توسم |

---

## 3. نموذج البيانات المفاهيمي

نموذج مفاهيمي، لا مخطط قاعدة بيانات. الحقول تخضع لحسم المسائل المفتوحة في `Luma.md`.

### `Document`

| الحقل | الغرض |
|---|---|
| `id` | معرف ثابت |
| `title` | عنوان المستخدم أو «بدون عنوان» |
| `blocks` | محتوى منظم — انظر أدناه |
| `createdAt` | بدء المستند الفعلي |
| `updatedAt` | آخر تعديل محفوظ |
| `lastOpenedAt` | ترتيب الاسترجاع والاستئناف |

### `Block` — قرار جوهري

المحتوى **قائمة كتل مرتّبة**، لا نصًّا منسَّقًا مؤرشفًا ولا RTF ولا HTML:

| الحقل | الغرض |
|---|---|
| `id` | معرف ثابت للكتلة |
| `role` | `body` أو `h1` أو `h2` — وما يُعتمد لاحقًا |
| `text` | نص عادي |
| `marks` | علامات داخل السطر — فارغة حتى تُعتمد مجموعة التنسيق المرشحة |

**لماذا** **ثابت**

- يضمن أن تغيير خط العرض أو الثيم **لا يغيّر بنية المحتوى** — وهو التزام منتج لا تفصيلًا تقنيًا.
- يجعل اللقطات قابلة للمقارنة والتخزين المضغوط.
- يمنع تسرّب قرارات العرض (الخط، الحجم، اللون) إلى الملف المحفوظ.
- يبقي الهجرة بين الإصدارات ممكنة وقابلة للاختبار.

النص المنسَّق يُبنى عند العرض من: الكتل + نمط الدور + تفضيل الخط + الثيم الحالي. هو مخرَج، لا مصدر.

### `Revision`

| الحقل | الغرض |
|---|---|
| `id` · `documentId` · `createdAt` | تعريف المرحلة |
| `snapshot` | لقطة كاملة أو فرق — **مفتوح** هندسيًا |
| `source` | `automatic` أو `beforeRestore` |
| `wordCount` | للعرض فقط |

### `Idea`

`id` · `text` · `sortOrder` · `createdAt` · `updatedAt` · و`documentId` اختياري ما دام ارتباطها مسألة مفتوحة.

### `Preferences`

| المجموعة | الحقول |
|---|---|
| المظهر | `themeId` |
| الكتابة | `fontSource` · `fontId` · `fontSize` (١٤–٢٦، خطوة ١) · `lineHeight` (١٫٤–٢٫٢، خطوة ٠٫١) · `columnWidth` (٥٢٠–٨٠٠) · `showWordCount` |
| التركيز | `typewriterEnabled` · `focusEnabled` · `zenBehavior` · `reduceMotionOverride` |
| الصوت | `soundsEnabled` · `activeSounds` · `masterVolume` |
| اللغة | `locale` |

### `FontReference`

`id` · `source` (`bundled` / `system` / `imported`) · `familyName` · `localReference` · `arabicCoverage`.

`arabicCoverage` معلومة معاينة وتحذير، **وليست سببًا لإخفاء الخط تلقائيًا**. **ثابت**

### كيانات لا تُضاف الآن **ثابت**

`User` · `Workspace` · `Project` · `Folder` · `Tag` · `Attachment` · `Goal` · `Keyword`. إضافة أيٍّ منها تعني ميزة غير معتمدة.

---

## 4. التخزين وإدارة الحالة

### التخطيط الفعلي **مثبَّت**

```text
~/Library/Application Support/Luma/
├── Documents/<uuid>/
│   ├── document.json
│   └── revisions/<timestamp>.json
├── ideas.json
├── fonts/                 ← نسخ ملفات الخطوط المستوردة
├── fonts.json             ← FontReference
└── preferences.json       ← التفضيلات، في مخزن منفصل عن المستندات
```

**لماذا ملف لكل مستند:** يحصر أثر أي تلف في مستند واحد، ويسمح بكتابة ذرّية مستقلة، ويجعل النسخ الاحتياطي على مستوى النظام مفهومًا.

**قواعد التخزين** **ثابت**

- كتابة ذرّية دائمًا: كتابة إلى ملف مؤقت ثم استبدال. لا تعديل في مكانه.
- البُفر في الذاكرة هو المرجع حتى ينجح الاستبدال.
- التفضيلات في مخزن منفصل، فلا يُفسد إعداد تالف نصًّا.
- الفهرس مشتق ويُعاد بناؤه عند التلف؛ لا يُعامَل كمصدر حقيقة.
- تُختبر الهجرة بين إصدارات البنية قبل أي تغيير في المخطط، وتُحفظ نسخة قابلة للاستعادة قبل أي تحويل.

### إدارة الحالة **توجه**

- حالة مراقَبة دقيقة الحبيبات: تغيّر قيمة يعيد رسم ما يعتمد عليها وحده، لا الشجرة كلها.
- `EditorSession` واحدة لكل مستند مفتوح، تملك الكتل والتحديد وحالة الحفظ.
- حالة اللوحات والأوضاع حالة عرض، لا محتوى دائم — عدا مساحة الأفكار.
- التخزين خلف مسار متسلسل واحد لكل مخزن؛ لا وصول للقرص من المسار الذي يعالج الإدخال.

---

## 5. الحفظ التلقائي

المسار الكامل، ولا يُسمح لأي خطوة منه بحجب الإدخال: **ثابت**

```text
إدخال → تحديث الكتل في الذاكرة (على مسار الإدخال، رخيص)
      → تجميع (debounce) + سقف زمني أقصى
      → تسلسل + كتابة ذرّية خارج مسار الإدخال
      → تحديث حالة الحفظ في الواجهة
```

| المعامل | قيمة مبدئية | الحالة |
|---|---|---|
| مهلة السكون قبل الكتابة | ٧٠٠ مللي ثانية | **مثبَّت** — [ADR ٠٠٠٥](docs/decisions/0005-autosave-timing.md) |
| سقف أقصى أثناء كتابة متصلة | ٥ ثوانٍ | **مثبَّت** — لا يُعاد ضبطه مع كل حرف، وهو ما يحدّ الخسارة |
| إعادة المحاولة بعد الفشل | ١ · ٣ · ٨ · ٢٠ ثانية | **مثبَّت** |
| عند إخفاء النافذة أو الخروج أو تبديل المستند | كتابة فورية | **ثابت** |

**عند الفشل** **ثابت**

- لا يُمحى البُفر من الذاكرة ولا يُستبدل ملف سليم بملف جزئي.
- تظهر حالة `تعذّر الحفظ` بأيقونة ونص ولون معًا، وتُعلَن لقارئ الشاشة من دون سحب التركيز.
- إعادة محاولة بتباعد متزايد، وعند إغلاق التطبيق تُعرض فرصة استرجاع صريحة.

**لا يوجد** زر حفظ، ولا اختصار حفظ يدوي في التدفق العادي، ولا إعداد لتعطيل الحفظ، ولا فاصل زمني ظاهر للمستخدم. **ثابت**

---

## 6. السجل الزمني

**محفّزات مقترحة للقطة** **توجه**

- أول محتوى فعلي في المستند.
- حد سكون بعد تغيّر جوهري (عتبة كلمات أو نسبة تغيّر — تُضبط بالقياس).
- قبل أي استعادة، مباشرة.
- نهاية الجلسة / إغلاق المستند.

**تدفق الاستعادة** **ثابت**

```text
اختيار نسخة → معاينة (قراءة فقط، لا حفظ، لا تحرير)
             → «استعادة هذه النسخة»
             → لقطة للحالة الحالية أولًا
             → تطبيق النسخة → كلتاهما في السجل
```

- إجراء استعادة واحد فقط في الواجهة، معطَّل ما دامت النسخة الحالية هي المحددة.
- تعذُّر قراءة لقطة قديمة يُظهر خطأ محصورًا في تلك اللقطة ولا يمسّ المستند.

**حُسم:** لقطة كاملة بلا ضغط، وحدّها ١٠٠ لقطة أو ٢٠MB لكل مستند أيّهما أسبق، وعتبة التغيّر ٨٠ حرفًا. مقيسًا على مستند ٢٠١٤٣ كلمة: المستند ٢٣٩KB واللقطة ٢٢٧KB. ما مصدره `beforeRestore` لا يُقصّ أبدًا. [ADR ٠٠٠٦](docs/decisions/0006-revision-format.md)

---

## 7. المحرر: النص والعربية وRTL

### الاتجاه **ثابت**

- واجهة التطبيق كلها تُخطَّط من اليمين إلى اليسار، وتُبنى على خصائص تخطيط منطقية (بداية / نهاية) لا على يمين ويسار صريحين.
- نص المحرر اتجاهه الأساسي من اليمين إلى اليسار، مُصرَّحًا به على مساحة النص لا مستنتَجًا من المحتوى.
- **Chrome النظام لا يُعكس.** تُستخدم شريط عنوان النافذة الأصلي وأزراره كما هي، ولا يُعاد رسمها ولا تُنقل. مكوّن «أزرار النافذة» في Figma لتمثيل اللقطة فقط.
  - **ملاحظة مقاسة:** حين تكون لغة النظام عربية يضع macOS أزرار كل نافذة يمينًا في كل التطبيقات. Luma لا تقاوم ذلك — المقاومة هي «النقل» الممنوع. لذلك تتبع الحشوة المحجوزة في شريط العنوان جانب الأزرار الفعلي، ولا تُثبَّت يسارًا. [ADR ٠٠٠٣](docs/decisions/0003-macos-rtl-window-chrome.md).
- ترتيب التركيز بلوحة المفاتيح منطقي في سياق RTL: من اليمين إلى اليسار ومن الأعلى إلى الأسفل.

### النص المختلط **ثابت**

- عند إدراج مقطع لاتيني داخل سلسلة واجهة عربية، يُعزل بمحدِّدات الاتجاه (`U+2066 LRI` … `U+2069 PDI`) أو يُتجنَّب الإدراج أصلًا.
- لا تُعرض امتدادات ملفات ولا صيغ وقت برموز محايدة داخل نص عربي — تُصاغ نصًّا («١٢ ثانية»).
- الأرقام: تنسيق بـlocale عربي لنصوص الواجهة، ولاتينية للسلاسل التقنية فقط.

### الآلة الكاتبة **توجه في التنفيذ، ثابت في السلوك**

بعد كل تغيير في التحديد أو النص: يُحسب مستطيل المؤشر، ثم تُمرَّر مساحة النص بحيث يستقر السطر النشط في نطاق مريح قرب الوسط. تُضاف حشوة علوية وسفلية تكفي ليصل السطر الأول والأخير إلى ذلك النطاق. التمرير هادئ، ويصبح فوريًا عند تفعيل تقليل الحركة.

**منفَّذ — الحساب في `src/lib/typewriter.ts` بلا DOM فيُختبر بلا متصفح.** النطاق ٣٦٪–٥٦٪ من الارتفاع ومرساته ٤٥٫٥٪، والحشوة ٤٥٫٥vh و٥٤٫٥vh. **وداخل النطاق لا يتحرّك شيء**: تثبيتٌ على نقطة يعني تمريرًا مع كل ضغطة — اهتزازٌ لا هدوء.

**ولا انزلاق أثناء الكتابة.** الأنيميشن يستغرق نحو ٣٠٠ms والكتابة تُطلق تغييرًا كل ١٠٠ms، فيُلغى ويُعاد بدؤه ويتخلّف العرض — قيس فبقي السطر ٢٣٨px تحت النطاق. الهدوء من النطاق لا من الأنيميشن، والانزلاق للنقلة الملاحية البعيدة وحدها. [ADR ٠٠١٠](docs/decisions/0010-comfort-and-settings.md)

### التركيز **ثابت**

يُطبَّق كسمات عرض مؤقتة على النطاقات غير النشطة — لا تعديل على `Block` ولا على النص المخزَّن. لون التخفيت رمز ثيم مقروء (`text/muted` أو ما يعادله)، لا شفافية عشوائية.

### Zen **ثابت**

تراجع عناصر الواجهة سلوك عرض. يبقى مخرج ظاهر يعمل بالمؤشر **وبلوحة المفاتيح**، ولا يُحبس التركيز داخل الوضع.

**منفَّذ:** العناصر تتراجع بصريًا ولا تُزال — إزالتها تنقل التركيز فجأة — و`:focus-within` يعيدها فورًا. و**الحركة تُقاس لا تُصدَّق**: WebKit يبثّ `pointermove` بإحداثيات لم تتغيّر بعد كل تمرير، فمؤشرٌ ساكن كان يُلغي Zen مع كل سطر يُكتب.

### التراجع **ثابت**

تجميع عمليات التراجع بحسب دفقة الكتابة لا الحرف الواحد. إدراج نص الإملاء عملية تراجع **واحدة** تزيل نص الجلسة كاملًا.

**مقاس:** نافذة التجميع ٥٠٠ms. دفقة متصلة من ٣٢ حرفًا تُزال بتراجع واحد، والتوقف يبدأ دفقة جديدة.

**مكدّس تراجع واحد فقط.** بندا «تراجع» و«إعادة» في قائمة macOS لا يكونان `PredefinedMenuItem`: محدِّد `undo:` يوقظ مدير التراجع في نافذة العرض فيتنازع مكدّسان على النص الواحد. البند يبثّ حدثًا والواجهة تنفّذه على مكدّس المحرر. القص والنسخ واللصق والتحديد تبقى عناصر نظام.

---

## 8. الخطوط

| المصدر | المطلوب |
|---|---|
| مدمج | Cairo وAlmarai داخل الحزمة، بلا أي طلب شبكة |
| النظام | تعداد عائلات الخطوط المثبَّتة في macOS بأسمائها الصحيحة، وإتاحتها للعرض داخل المحرر |
| مستورد | نسخ الملف إلى `Application Support/Luma/fonts/` وإتاحته للعرض، وإعادة إتاحته عند كل إقلاع |

**فحص التغطية العربية:** قراءة جدول محارف الخط ومقارنته بمجموعة عيّنة تغطي الحروف العربية والهمزات والتشكيل والأرقام العربية الهندية. النتيجة **معلومة للمستخدم**، لا فلترة تلقائية. **ثابت**

**منفَّذ:** `CTFontGetGlyphsForCharacters` يُسأل عن مِحرف كل رمز — صفرٌ يعني أنه ليس في الجدول. ثلاث درجات: كاملة · ناقصة · بلا عربية. قياسًا على النظام: من ٢٠٦ عائلة، ٣٥ بتغطية كاملة و١٧١ بلا عربية.

**سلسلة الرجوع** **ثابت**

```text
الخط المختار → (إن نقصت التغطية العربية) Almarai للنطاقات العربية
             → (إن اختفى الخط) Almarai + إشعار خافت
```

**حالات إلزامية:** ملف تالف يُرفض بوضوح بلا أثر على المستند · خط بلا تغطية عربية يُقبل مع تحذير ورجوع للعربية · خط اختفى من النظام بعد اختياره يعود بأمان. الثلاث مختبَرة على ملفات ونظام حقيقيين في `src-tauri/tests/fonts.rs`.

**حُسم — [ADR ٠٠٠٩](docs/decisions/0009-fonts.md):**

| البند | القرار |
|---|---|
| الصيغ | `OTF` · `TTF` · `TTC` · `OTC`. **لا WOFF2**: لا يسجّلها مدير خطوط النظام، فقبولها وعدٌ كاذب |
| حد الحجم | ٢٠ ميغابايت |
| ترتيب الاستيراد | امتداد ← حجم ← **حكم النظام على الملف** ← نسخ ← تسجيل. لا يُنسخ ملف إلى مجلد المستخدم قبل أن يثبت أنه خط |
| نطاق التسجيل | **العملية** لا الدائم: Luma لا تثبّت خطوطًا في نظام المستخدم |

**كيف تصل إلى طبقة العرض — وما اكتُشف بالقياس.** خطوط النظام يكفي اسمها في `font-family`. أما المستورد فـ**التسجيل وحده لا يكفي**: WebKit يرسم في عملية محتوى مستقلة لا ترث تسجيل نطاق العملية. قيس بخط مسبار داخل `Luma.app`: مسجَّلًا وحده رُسم بعرض البديل نفسه (٣١٨px)، ومع `@font-face` رُسم بـ٣٦١px. فيُعلَن للعرض بـ`@font-face` عبر بروتوكول الأصول بنطاق محصور في مجلد خطوط Luma، ويبقى التسجيل لأن قراءة التغطية في النواة تحتاجه.

**بلا شبكة في المصادر الثلاثة** — شرط المسألة، مُتحقَّق منه.

---

## 9. الثيمات وربط الرموز

الثيم بيانات لا كود موزَّع. تُحمَّل خمس لوحات، وتُختار بـ`themeId`، ولا يُكتب لون واحد يدويًا في الواجهة. **ثابت**

**منفَّذ:** المصدر الوحيد `src/tokens/source.json` مستخرج من مجموعات Figma الثلاث، ويولّد `tokens.css` و`themes.ts`. الثيم يُطبَّق بسمة واحدة على الجذر، فلا وميض ولا فقد تمرير أو مؤشر. حرّاس آلية تمنع اللون الحرفي والمتغيّر غير المعرَّف، وتفرض التباين على ٢١ زوجًا × ٥ ثيمات. [ADR ٠٠٠٧](docs/decisions/0007-token-layer.md)

أسماء الرموز في الكود تطابق أسماء متغيّرات Figma واحدًا لواحد:

| Figma | رمز الكود | الاستخدام |
|---|---|---|
| `surface/paper` | `surface-paper` | ورقة الكتابة، البطاقات، اللوحات |
| `surface/canvas` | `surface-canvas` | خلفية النافذة |
| `surface/raised` | `surface-raised` | رؤوس اللوحات والأشرطة |
| `surface/sunken` | `surface-sunken` | الحقول والمسارات |
| `border/subtle` | `border-subtle` | حدّ زخرفي — **لا يُستخدم مؤشرًا وحيدًا لعنصر تحكم** |
| `border/strong` | `border-strong` | فواصل المناطق |
| `border/control` | `border-control` | حدّ عناصر التحكم — محلول لـ٣:١ |
| `text/primary` · `secondary` · `muted` | `text-primary` … | نص الواجهة بمستوياته |
| `text/on-accent` | `text-on-accent` | نص فوق تعبئة اللمسة أو الحالات |
| `editor/ink` | `editor-ink` | حبر نص المحرر |
| `accent/graphic` | `accent-graphic` | الرموز والأيقونات ومسارات المنزلقات وحلقة التركيز |
| `accent/text` | `accent-text` | نص اللمسة وتعبئة الزر الأساسي |
| `accent/subtle` | `accent-subtle` | خلفية الصف المحدد والشارة |
| `accent/selection` | `accent-selection` | خلفية تحديد النص في المحرر |
| `state/{positive,caution,critical,info}` + `-bg` | `state-positive` … | الحالات وتظليلاتها |

**قواعد الاستخدام** **ثابت**

- `accent-graphic` للرسوميات وحلقات التركيز فقط، ولا يُستخدم خلفيةً خلف نص.
- الصف المحدد = `accent-subtle` + `accent-text`. الزر الممتلئ = `accent-text` + `text-on-accent`.
- حلقة التركيز: إطار خارجي ٢px بلون `accent-graphic` بإزاحة ٢px، لا تُلغى أبدًا لأسباب جمالية.
- لا معنى يُنقل باللون وحده: كل حالة تحمل أيقونة أو نصًّا معها.

### أنماط النص

| Figma | رمز الكود | القيمة |
|---|---|---|
| `واجهة · UI/01–10` | `ui-01` … `ui-10` | Cairo، من ٤٨/٦٢ إلى ١٢/١٨ |
| `محرر · Editor/02` | `editor-subtitle` | Almarai Bold ٢٤/٣٨ |
| `محرر · Editor/05` | `editor-quote` | Almarai Light ١٩/٣٦ |
| `محرر · Editor/01` | `editor-title` | Almarai Bold ٣٢/٤٨ |
| `محرر · Editor/03` | `editor-reading` | Almarai Regular ١٩/٣٦ |
| `محرر · Editor/04` | `editor-comfort` | Almarai Regular ١٩/٤٠ |
| `محرر · Editor/06` | `editor-note` | Almarai Regular ١٥/٢٦ |

القياسات في Figma بالبكسل، وعلى macOS ١pt = ١px عند ١×. حجم نص المحرر وتباعده يضربهما تفضيل المستخدم؛ أنماط الواجهة ثابتة.

**أنماط «توثيق» لا تُشحن:** فيها تتبّع موجب (٤٪ و٨٪) يخالف قاعدة «التتبّع صفر دائمًا» على العربية. هي أنماط صفحات دليل في Figma، ولا تدخل طبقة الرموز.

### مقاسات ثابتة من التصميم

| العنصر | القيمة |
|---|---|
| شريط النافذة | ٤٨ |
| شريط الأسطح | ٥٢ |
| ورقة المستند | عرض ٨٨٠، حشوة جانبية ٤٠ |
| عمود النص | ٨٠٠ — وهو سقف المدى ٥٢٠–٨٠٠، فالافتراضي عند أقصاه والضبط ينزل به. **لا يتمدّد بعده مهما اتسعت النافذة**، والنافذة الضيّقة تُقلّصه نحو ٥٢٠ أولًا ثم تُعطي حشوةُ الورقة لتُبقيه عنده |
| اللوحة الجانبية | ٣٢٠ |
| الأيقونة داخل منطقة النقر | ١٦ داخل ٣٢×٣٢ |
| الزر | كبير ٤٤ · متوسط ٣٦ · صغير ٢٨ — حشوة أفقية ٢٠ · ١٦ · ١٢ |
| المفتاح | مسار ٤٤×٢٤، مقبض ٢٠، منطقة نقر ٤٤×٤٤ |
| المنزلق | مسار ٤، مقبض ١٦، منطقة نقر ٣٢×٣٢ |
| نصف القطر | ٤ · ٨ · ١٢ · ١٦ · ٢٤ · كامل |
| المسافات | سلّم من ١٤ خطوة: ٢ · ٤ · ٨ · ١٢ · ١٦ · ٢٠ · ٢٤ · ٣٢ · ٤٠ · ٤٨ · ٦٤ · ٨٠ · ٩٦ · ١٢٨ |
| الارتفاع | مستويان: «ورقة» للورقة والبطاقات، و«طبقة» للوحات العائمة. في ثيم «ليل» تعتمد الطبقات على فرق السطح أكثر من الظل |

**حلقة التركيز إطار (`outline`) لا ظل:** حدّ خارجي ٢px بلون `accent-graphic` بإزاحة ٢px. تُنفَّذ داخل كل مكوّن، ولا يوجد لها نمط تأثير مستقل. **ثابت**

---

## 10. الأسطح الجانبية والأوضاع

**قاعدة واحدة تحكم كل الأسطح:** فتح لوحة يضيف عمودًا بعرض ٣٢٠ ويعيد تمركز عمود النص في المساحة المتبقية. **لا يتغيّر** المستند ولا الورقة ولا شريط النافذة ولا موضع المؤشر. **ثابت**

| السطح | الحالات المطلوبة |
|---|---|
| المكتبة | ممتلئة · فارغة · بلا نتائج بحث |
| مساحة الأفكار | تحتوي عناصر · فارغة |
| السجل | نسخ متاحة · معاينة · لا نسخ |
| البيئة الصوتية | كل الأصوات متوقفة · صوت نشط · اللوحة مغلقة مع صوت يعمل |
| الإملاء | طلب الإذن · يستمع · متوقف مؤقتًا · يعالج · تعذّر · اكتمل |

لكل لوحة **مدخل واحد** في شريط الأسطح، **وهو نفسه ما يغلقها**، و`Esc` يغلق أي لوحة مفتوحة. لا لوحة تحبس التركيز، ولا تحجب العنصر الذي يحمله. **ثابت**

**لا ترويسة داخل اللوحة.** كانت القاعدة «زر إغلاق ظاهر داخلها»، فأنتجت عنوانًا مكرَّرًا وزرَ إغلاق ثانيًا فوق كل عمود — ضجيجٌ في سطح غايته الهدوء، ومخرجان لفعل واحد. المخرج الآن المفتاحُ نفسه أو `Esc`، واسم اللوحة يصل قارئ الشاشة عبر `aria-label` على العمود. `Luma.md` §٦ **ثابت** — [ADR ٠٠٠٨](docs/decisions/0008-shell-and-surfaces.md).

**منفَّذ:** `EditorShell` مكوّن واحد تُبنى عليه كل شاشة، واللوحة عمود يُضاف إلى جسمه. المكتبة والسجل مبنيّتان؛ والأفكار والبيئة الصوتية والإملاء خارج نطاق MVP، **ولا تُعرض مداخلها**: مدخلٌ لا يفتح شيئًا وعدٌ كاذب. مكوّن الشريط يحتمل الخمسة بلا تعديل (`src/lib/surfaces.ts`). [ADR ٠٠٠٨](docs/decisions/0008-shell-and-surfaces.md)

**كيف يُحفظ موضع التمرير:** بألّا يتغيّر عرض الورقة أصلًا. عرض ثابت ⇒ لا إعادة لفّ للسطور ⇒ ارتفاع المحتوى نفسه ⇒ `scrollTop` يشير إلى الموضع نفسه. لا حفظ ولا إعادة بعد التغيير.

**التركيز:** اللوحة أول الجسم في الشجرة، فهي عند بداية القراءة بصريًا وهي التالية لمدخلها في ترتيب Tab. **الفتح لا ينقل التركيز** — إخفاء المؤشر من مساحة الكتابة بلا طلب فقدٌ له. والإغلاق يعيده إلى النص بتحديده كما كان.

**المعاينة:** `EditorCore.setEditable(false)` يوقف التحرير ويُبقي التحديد والنسخ، ومؤشر الحفظ يختفي. الخروج من المعاينة يعيد النص الحيّ من الجلسة — لم يغادرها أصلًا.

---

## 11. الصوت والإملاء والخصوصية

### الصوت **توجه**

تشغيل حلقي لثلاثة أصول صوتية مدمجة، بمستوى واحد ومخرج إيقاف شامل بفعل واحد. لا يقاطع صوت النظام ولا صوت التطبيقات الأخرى. متوقف عند أول تشغيل. حالته في التفضيلات.

### الإملاء **توجه**

تعرّف على الكلام العربي من الميكروفون. صلاحية الميكروفون وصلاحية التعرّف تُطلبان عند أول استخدام فقط، لا عند الإقلاع.

- النتائج الجزئية تُعرض بتمييز بصري ولا تُثبَّت في `Block`.
- النتيجة النهائية تُدرج عند موضع المؤشر كعملية تراجع واحدة.
- لا يُحتفظ بأي صوت، ولا يُنشأ مرفق، ولا يعمل الميكروفون في الخلفية. **ثابت**
- قدرة المحرك على المعالجة **على الجهاز** هي ما يحسم مسألة موضع المعالجة في `Luma.md`. محرك لا يوفّرها يفرض قرار خصوصية صريحًا قبل استخدامه.

### الخصوصية — خط أساس **ثابت**

- نص المستخدم لا يُحلَّل خارج سياق التحرير والحفظ والاستعادة.
- لا تسجّل السجلات التشخيصية نص المستند ولا عنوانه ولا الأفكار ولا ناتج الإملاء.
- ملفات الخطوط المستوردة بيانات خاصة بالتطبيق ولا تُرفع لأي خدمة.
- الحذف والاستعادة ومدة الاحتفاظ مفهومة للمستخدم قبل الإطلاق.
- **لا يُكتب في الواجهة أي بيان عن موضع معالجة الصوت قبل حسم القرار.** ادعاء غير مسنود أسوأ من الصمت.

**افتراض MVP المحافظ:** تخزين على الجهاز، بلا حساب وبلا مزامنة. أي تغيير يحتاج قرارًا يحدد المزامنة والتشفير والتعارضات وسياسة الاحتفاظ.

---

## 12. من Figma إلى الكود

الملف: `Luma — Write Without Noise`. الصفحات ٠٩–١٦ هي الشاشات والحالات، والصفحة ١٧ هي مصدر المكونات والرموز وقواعد التسليم، والصفحة ١٩ هي النموذج التفاعلي الموصول. وصف كل مكوّن في Figma يحمل قواعده وحدوده — يُقرأ قبل بنائه.

| مكوّن Figma | مقابله المقترح | ملاحظة |
|---|---|---|
| `إطار المحرر · Editor Shell` | ✅ `EditorShell` | يجمع شريط النافذة والأسطح والورقة، ويحمل عمود اللوحة |
| `ورقة المستند` | ✅ داخل `EditorShell` | لم تُفصل مكوّنًا: لا تُستعمل خارج الإطار، ولا حالات لها |
| `شريط الأسطح` | ✅ `SurfacesBar` | يحتمل الخمسة، ويعرض المنفَّذ منها فقط |
| `حالة الحفظ · Save Status` | `SaveStatusView` | ثلاث حالات، لا زر |
| `شريط التحديد · Selection Toolbar` | ✅ `SelectionToolbar` | يظهر مع التحديد فقط — عادي وH1 وH2 و«» لا غير |
| `زر المحرر المريح · Comfort Button` | `ComfortButton` — **لا يُعرض قبل المرحلة ٦** | المكوّن جاهز في المعرض، ولا يدخل الشاشة قبل سلوكه: زرٌّ لا يفعل شيئًا يشغل مكانًا ويكذب |
| `لوحة المكتبة · Library Panel` + `صف مستند · Document Row` | ✅ `LibraryPanel` + `DocumentRow` | ثلاث حالات للوحة، وأربع للصف |
| `حقل بحث · Search Field` | ✅ `SearchField` | الأيقونة أول العنصر وزر المسح آخره — منطقيًّا لا بالجهات |
| `حقل نص · Text Field` | `LumaTextField` | ثماني حالات — نطاقه اسم المستند والبحث فقط |
| `لوحة الأفكار · Ideas Panel` + `بطاقة فكرة · Idea Card` | `IdeasPanel` + `IdeaCard` | البطاقة تعرض النص والوقت فقط |
| `لوحة السجل · History Panel` + `نقطة سجل · Revision Item` | ✅ `HistoryPanel` + `RevisionRow` | «قيد المعاينة» = قراءة فقط، وإجراء استعادة واحد في التذييل |
| `لوحة البيئة الصوتية · Ambience Panel` | `AmbiencePanel` | |
| `شريط الإملاء · Dictation Bar` | `DictationBar` | خمس حالات |
| `ترويسة لوحة · Panel Header` | ❌ **لا يُبنى** | المفتاح الذي يفتح هو الذي يغلق — `Luma.md` §٦ |
| `زر · Button` | `LumaButton` + `ButtonKind` | أربعة أنواع × ست حالات |
| `زر أيقونة · Icon Button` | `IconButton` | تسمية وصفية إلزامية |
| `مفتاح` · `خانة اختيار` · `زر اختيار` · `منزلق` | `Toggle` · `Slider` … بأنماط مخصصة | **تُستخدم عناصر التحكم الأصلية في المنصة بنمط مخصص، لا عناصر مرسومة** — لأن الوصول والاختصارات يأتيان معها |
| `تنبيه · Alert` | `InlineNotice` | غير حاجب، يبقى حتى يُعالج سببه |
| `إشعار عابر · Toast` | `Toast` | واحد في كل وقت، لا يتراكم |
| `شارة · Badge` | `Badge` | لا تنقل حالة حرجة وحدها |
| `صف إعداد` · `صف تنقل` · `بطاقة ثيم` | `SettingsRow` · `SettingsNavRow` · `ThemeCard` | |
| `أيقونة · Icon` (٢٢) | مجموعة أصول موحّدة من ملفات SVG | لون موروث، ١٦×١٦ |
| `أزرار النافذة · Window Controls` | — | **لا يُبنى.** يُستخدم شريط النافذة الأصلي |
| `رمز` · `تركيب` · `أيقونة التطبيق` (صفحة ٠٠) | أصول تصديرية | أيقونة التطبيق مصدرها ١٠٢٤ مربع غير مقصوص |

**قاعدة الاستيراد:** ما يوجد في الصفحة ١٧ يُبنى مرة واحدة كمكوّن قابل لإعادة الاستخدام. أي شاشة تحتاج شكلًا غير موجود فيها = إشارة إلى نقص في المكتبة، لا إلى استثناء في الشاشة. **ثابت**

**ثلاثة أشكال في الشاشات لا مقابل لها في الصفحة ١٧، وكيف عولجت** — [ADR ٠٠٠٨](docs/decisions/0008-shell-and-surfaces.md):

| الشكل | المكوّن المستعمل |
|---|---|
| رقاقة عداد الكلمات | `شارة · Badge` نوع «محايد» |
| شريط المعاينة (`120:353`) | `تنبيه · Alert` نوع «معلومة» — الدلالة نفسها: غير حاجب يبقى حتى يُعالج سببه |
| الحالة الفارغة في المكتبة والسجل | `EmptyState` — الشكل نفسه في `110:155` و`112:334`، فبُني مرة |

**حيث يتعارض التصميم مع الوثيقتين، الوثيقتان أعلى:**

- **الظل** يرفع `شريط التحديد` و`زر المحرر المريح` في Figma. الظل لون، ولا رمز ظلّ في المجموعات الثلاث — فالرفع بفارق السطح وحدّ `border/strong`، وهو ما يصفه §٩ لثيم «ليل».
- **«أمس ١١:٤٥ م»** في صفوف السجل. `Luma.md` §١٦ **ثابت** يمنع صيغ الوقت برموز محايدة، فكل زمن في الواجهة نسبيّ بالكلمات (`src/lib/bidi.ts`).
- **H3 وغامق وقائمة** في `شريط التحديد`. موسومة **مرشحة** في `Luma.md` §٥، فلم تُبنَ.
- **«أزرار النافذة يسارًا دائمًا»** في وصف `إطار المحرر`. نسختها [ADR ٠٠٠٣](docs/decisions/0003-macos-rtl-window-chrome.md): الموضع يقرّره النظام، والطرف المحجوز يتبعه.

---

## 13. الوصول

الهدف: **WCAG 2.2 AA** حيث ينطبق، وأعراف الوصول الأصلية في macOS. **توجه** كمعيار إصدار، **ثابت** كاتجاه.

- كل وظيفة أساسية متاحة بلوحة المفاتيح، وترتيب التركيز منطقي في RTL.
- مؤشر التركيز ظاهر دائمًا ولا يُحذف.
- كل زر أيقونة يحمل تسمية وصفية عربية واضحة تصل إلى قارئ الشاشة.
- حالات الحفظ والإملاء تُعلَن لقارئ الشاشة بأولوية منخفضة، **من دون نقل التركيز من النص**.
- الأهداف التفاعلية ٣٢×٣٢ فعليًا، والحد الأدنى المطلق ٢٤×٢٤.
- تقليل الحركة: إعداد النظام يُقرأ ويُحترم، فيتحوّل كل انتقال إلى تغيّر فوري بلا فقد وظيفة، ويتوقف نبض مؤشري الحفظ والإملاء.
- تكبير نص النظام وواجهته لا يقصّ الأوامر الأساسية ولا يكسر المحرر.
- مساحة النص تُعلن نفسها لقارئ الشاشة منطقةَ تحرير نصية حقيقية، ويقرأ VoiceOver محتواها العربي — لا تُستبدل بعنصر مرسوم.

**تحقق قبل الإصدار:** اجتياز كامل بلوحة المفاتيح · VoiceOver بالعربية ونص مختلط · مراجعة تباين كل رمز في الثيمات الخمسة · تكبير النص والواجهة · تقليل الحركة · حالات الخطأ والحفظ والإملاء لا الحالة المثالية فقط.

نتيجة هذا التحقق بندًا بندًا في [تقرير الجودة](docs/quality-report.md)،
وما لا تحكم عليه الأتمتة في [قائمة التحقق اليدوية](docs/release-checklist.md)،
وسلوك كل حالة خطأ في [سلوك حالات الخطأ](docs/error-behaviour.md).

**قيدٌ من المنصة يُعلَن ولا يُخفى:** الاجتياز بـ`Tab` على macOS يحكمه
إعداد النظام «التنقل بلوحة المفاتيح»، وهو مطفأ افتراضيًا فلا يصل `Tab`
إلا الحقول النصية — في Luma وفي كل تطبيق macOS أصلي. البنية سليمة
ومُختبَرة، والإعداد بيد المستخدم.

---

## 14. الأداء والاعتمادية

**مبادئ** **ثابت**

- تظهر مساحة الكتابة قبل تحميل الفهارس والأسطح الثانوية.
- استجابة الكتابة فورية إدراكيًا حتى مع نص عربي طويل.
- لا يعمل الحفظ ولا اللقطات ولا الصوت ولا البحث على مسار يجمّد المؤشر.
- لا يُعاد رسم المستند كله مع كل حرف.
- فتح لوحة أو تبديل ثيم أو خط لا يفقد موضع التمرير ولا التحديد.
- الدخول إلى المحرر المريح والخروج منه بلا قفزة تخطيطية.
- تُختبر الاستعادة بعد إغلاق قسري وبعد قطع عملية كتابة.
- تُكتشف البيانات غير القابلة للقراءة ويُمنع استبدال نسخة سليمة بها.

### الميزانيات **مثبَّتة ومحقَّقة** — [ADR ٠٠١١](docs/decisions/0011-performance-budgets.md)

مقيسة **داخل `Luma.app` مبنية** على جهاز macOS مستهدف، لا مأخوذة من
Lighthouse ولا من خادم تطوير. يقارنها الفحص الذاتي عند كل تشغيل ويسقط
عند التجاوز: `npm run selftest`.

| الميزانية | مقيس | السقف |
|---|---|---|
| زمن الفتح حتى مؤشر قابل للكتابة | ٤٨٦–٦٨٤ms | **١٢٠٠ms** |
| زمن ظهور الحرف p95 — مستند ٢٠ ألف كلمة | ٧–٨ms | **١٦ms** |
| زمن ظهور الحرف p95 — والآلة الكاتبة والتركيز مفعَّلان | ٨ms | **١٦ms** |
| زمن بناء مستند ٢٠ ألف كلمة في المحرر | ٣١٢ms | **٦٠٠ms** |
| زمن حفظ تعديل نموذجي | ٨–٩ms | **١٠٠ms** |
| نمو الذاكرة المقيمة في جلسة ممتدة | لا نمو | **٥٠MB** |
| زمن فتح مستند من مكتبة كبيرة | ١٤–٣٢ms | **٢٠٠ms** |
| حدّ حجم المستند المدعوم | ٢٠١٤٣ كلمة | **٢٠٠٠٠ كلمة** |
| حدّ عدد المستندات المدعوم | ٥٠٧ | **٥٠٠ مستند** |

الأرقام في `src/dev/budgets.ts`، **ويحرس تطابقها مع هذا الجدول اختبار**:
رقمٌ يتغيّر في أحدهما دون الآخر يُسقط البناء.

**لم تُقَس بعد** على أقدم جهاز مدعوم (macOS 13.0) — بندٌ في
[قائمة التحقق](docs/release-checklist.md) قبل الإطلاق.

---

## 15. الاختبارات

| المجال | ما يُختبر |
|---|---|
| العربية والاتجاه | المؤشر، التحديد، التراجع، النسخ واللصق، الالتفاف، النص المختلط، محدِّدات الاتجاه |
| الحفظ | التجميع، الكتابة الذرّية، مقاطعة الكتابة، الفشل وإعادة المحاولة، التعافي بعد إغلاق قسري |
| السجل | إنشاء اللقطة، المعاينة كقراءة فقط، لقطة ما قبل الاستعادة، لقطة تالفة |
| الخطوط | ملف تالف، خط بلا تغطية عربية، خط اختفى، إعادة الإتاحة بعد الإقلاع |
| الثيمات | لقطات مرجعية للثيمات الخمسة، وفحص تباين آلي لكل زوج رمز |
| الأسطح | فتح وإغلاق كل لوحة من مدخلها ومن زرها ومن `Esc` بلا فقد موضع المؤشر |
| الأوضاع | تمركز السطر في الآلة الكاتبة، تخفيت التركيز، مخرج Zen بلوحة المفاتيح |
| الوصول | تدفق كامل بلوحة المفاتيح، شجرة الوصول (أدوار وتسميات)، حلقة التركيز على **كل** عنصر، أهداف ٣٢×٣٢، تقليل الحركة بمصدريه، تكبير الواجهة — `tests/e2e/access.spec.ts`. وVoiceOver يدويًّا |
| الأداء | مستند ٢٠ ألف كلمة، مكتبة ٥٠٠ مستند، جلسة ممتدة — تسع ميزانيات مبوَّبة في `npm run selftest` |
| حالات الخطأ | فشل الكتابة داخل `write_atomic`، قرص ممتلئ، مستند تالف، لقطة تالفة، مخطط أحدث، قصّ اللقطات — وسلوكها موثَّق في [`docs/error-behaviour.md`](docs/error-behaviour.md) |

---

## 16. تسلسل بناء MVP

ترتيب يقلّل إعادة العمل: كل مرحلة تُنتج ما تحتاجه التي بعدها. **توجه**

| # | المرحلة | تنتهي عندما |
|---|---|---|
| ١ | **الأساس** — الرموز، الثيمات الخمسة، أنماط النص، الخطوط المدمجة، بنية الحزمة | تبديل الثيم يعمل ولا يوجد لون مكتوب يدويًا |
| ٢ | **نواة المحرر** — `EditorCore`، الكتل، RTL، التراجع | تُكتب فقرة عربية طويلة بسلوك macOS الطبيعي |
| ٣ | **الاستمرارية** — التخزين، الحفظ التلقائي، حالات الحفظ، التعافي | إغلاق قسري لا يفقد نصًّا، ولا يوجد زر حفظ |
| ٤ | **الأسطح** — إطار المحرر، شريط الأسطح، المكتبة، السجل، شريط التحديد | فتح أي لوحة لا يغيّر المحرر ولا موضع المؤشر |
| ٥ | **التركيز والتخصيص** — المحرر المريح بطبقاته، الإعدادات، خطوط النظام والمستوردة | الطبقات الثلاث تعمل ولها مخرج بلوحة المفاتيح |
| ٦ | **الجودة** — الوصول، تقليل الحركة، الأداء، الاختبارات | قائمة التحقق في القسم ١٣ مجتازة |

المؤجَّل إلى ما بعد MVP وفق `Luma.md`: مساحة الأفكار · الإملاء · الأصوات · البحث. بنيتها موجودة في التصميم، ونقاط ارتباطها في المعمارية محجوزة، وبناؤها لا يغيّر ما سبق.

هذا الجدول هو الترتيب المفاهيمي. المسار التنفيذي بمراحله واعتمادياته ومعايير اكتماله في [PLAN.md](PLAN.md)، وهو ما يُنفَّذ فعلًا.

---

## 17. مبادئ حاكمة

قائمة قصيرة تصلح للمراجعة قبل أي دمج: **ثابت**

1. لا يحجب الحفظ الإدخال، ولا يحرّك المؤشر.
2. لا يُستبدل ملف سليم بملف جزئي في أي مسار.
3. تغيير الخط أو الثيم لا يمسّ المحتوى المخزَّن.
4. فشل أي خدمة ثانوية لا يمنع الكتابة ولا الحفظ ولا الاستعادة.
5. الاستعادة تحفظ الحالة الحالية قبل استبدالها.
6. لا لون مكتوب يدويًا؛ كل لون رمز ثيم.
7. RTL يُبنى في المكوّن، ولا يُعكس Chrome النظام.
8. كل عنصر تفاعلي له: حالة تركيز ظاهرة، تسمية وصفية، ومسار بلوحة المفاتيح.
9. لا معنى يُنقل باللون وحده.
10. لا يُضاف كيان بيانات لميزة غير معتمدة في `Luma.md`.

---

## 18. مسائل تقنية مفتوحة

تُحسم بالقياس أو التجربة، ولا يُبنى عليها افتراض صامت.

| # | المسألة | ما يحسمها |
|---|---|---|
| ~~١~~ | ~~تثبيت الغلاف الحالي~~ | ✅ [ADR ٠٠٠١](docs/decisions/0001-shell-tauri.md) |
| ~~٢~~ | ~~أساس نواة تحرير النص~~ | ✅ [ADR ٠٠٠٢](docs/decisions/0002-editor-core.md) |
| ~~٣~~ | ~~الحد الأدنى لإصدار macOS المدعوم~~ | ✅ [ADR ٠٠١٢](docs/decisions/0012-minimum-macos.md) — 13.0، محسوبًا من جرد خصائص المنصة |
| ٤ | صيغة تخزين المستند وموقعه ومستوى الحماية المعتمد على النظام | قرار هندسة + مراجعة خصوصية |
| ~~٥~~ | ~~شكل تخزين اللقطة~~ | ✅ [ADR ٠٠٠٦](docs/decisions/0006-revision-format.md) |
| ~~٦~~ | ~~معاملات التجميع الزمني للحفظ~~ | ✅ [ADR ٠٠٠٥](docs/decisions/0005-autosave-timing.md) |
| ٧ | الشق التقني من مسألة الإملاء في `Luma.md`: اختيار المحرك وقياس الدقة وزمن الاستجابة | تجربة مقارنة على عيّنات عربية |
| ~~٨~~ | ~~الشق التقني من مسألة الخطوط~~ | ✅ [ADR ٠٠٠٩](docs/decisions/0009-fonts.md) — قيس داخل `Luma.app` على ٢٠٦ عائلة نظام وخط مسبار مستورد |
| ٩ | تخزين مساحة الأفكار ونطاق ارتباطها بالمستند | يتبع القرار المنتجي في `Luma.md` |
| ~~١٠~~ | ~~ميزانيات الأداء وحدود حجم المستند والمكتبة كبوابة إطلاق~~ | ✅ [ADR ٠٠١١](docs/decisions/0011-performance-budgets.md) — تسع ميزانيات مقيسة داخل `Luma.app` وتبوّبها البوابة |
| ~~١١~~ | ~~أقل قدر من القياس التشخيصي، وطريقة تعطيله أو الموافقة عليه~~ | ✅ [ADR ٠٠١٥](docs/decisions/0015-no-telemetry.md) — أقلّ قدر هو **الصفر**، ولا شبكة أصلًا: يفرضه ثلاث طبقات ويحرسه اختبار |
| ١٢ | سياسة الهجرة بين إصدارات بنية البيانات | مسارها مثبَّت ومختبَر من أول يوم (`storage/migrate.rs`)؛ أول خطوة فعلية تُكتب عند أول تغيير في المخطط |
| ~~١٣~~ | ~~موضع أزرار النافذة حين تكون لغة النظام عربية~~ | ✅ [ADR ٠٠٠٣](docs/decisions/0003-macos-rtl-window-chrome.md) — صيغة §١٦ صُحّحت |
