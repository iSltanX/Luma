# سجل التغييرات

كل إصدار وما تغيّر فيه. الترقيم [دلالي](https://semver.org/lang/ar/):
`الرئيسي.الثانوي.التصحيحي`. والصيغة المخزَّنة لا تتغيّر داخل الرئيسي
الواحد إلا بهجرة — **ومسار الهجرة مثبَّت ومختبَر من أول يوم**
(`storage/migrate.rs`).

---

## 1.1.0 — ٢٧ أغسطس ٢٠٢٦

أول إصدار. محرر كتابة عربي على macOS: افتح واكتب، والباقي يختفي.

### الكتابة

- محرر على ProseMirror، عربي RTL بالبناء لا بالقلب.
- ثلاثة أدوار للكتل: فقرة وعنوان أول وعنوان ثانٍ، وعلامة اقتباس عربية.
  لا غامق ولا مائل — **العربية لا تُمال**.
- **زرّا التراجع والإعادة** في شريط الأسطح — مدخلٌ ثانٍ إلى مكدّس
  التراجع نفسه الذي تصله لوحة المفاتيح، لا مسارٌ موازٍ. التراجع بدفقة
  كتابة لا بحرف، والمعاينة تحفظ المكدّس فلا يضيع عند العودة منها.
- اللصق ينظّف محارف الاتجاه المدسوسة ولا يُدخل تنسيقًا.
- تتبّع صفر على كل نص عربي، وارتفاع سطر مصرَّح به دائمًا.

### الحفظ والاستمرارية

- **حفظ تلقائي بلا زر حفظ**: تجميع ٧٠٠ms وسقف ٥ ثوانٍ.
- كتابة ذرّية: لا يُستبدل ملف سليم بجزئي في أي مسار.
- كل تشغيل يفتح مساحة كتابة نظيفة — لا يبدأ من المكتبة، وما كُتب يُفتح منها بيد الكاتب.
- سجل زمني بلقطات، ومعاينة للقراءة فقط، ولقطة أمان قبل كل استعادة.
- مكتبة جانبية ببحث يطبّع الهمزة والألف والياء والتاء المربوطة والتشكيل.
- **زرّ الحذف** في شريط الأسطح، واختصار وبند قائمة — والحذف يصير
  **تدارَكًا لا محوًا**: المستند ينتقل إلى **السلّة** لا يُفنى، ويُستعاد
  منها أو يُترك حتى يُفرَغ. السلّة محمية من التسابق: استعادة عنصر
  وإفراغ السلّة في اللحظة نفسها لا يتصادمان — قفلٌ بنيوي في طبقة
  التخزين، لا اتفاقًا بين النداءات.

### التصدير

- **حفظ بصيغة…** في قائمة «ملف»، فوق «حذف النص» بفاصل — لا زرّ في
  شريط الأسطح ولا اختصار: فعلٌ نادر لا يستحق مكانًا دائمًا.
- **Markdown ونصّ عادٍ**: يُصدَّران مما هو معروض على الشاشة فعليًا —
  لا من آخر ما وصل القرص — والعنوان المشتقّ يدخل الملف ترويسةً بلا
  تكرار. الكتابة عبر لوحة حفظ النظام: لا مسار يصل من الواجهة، ولا
  صلاحية جديدة.
- **PDF عبر لوحة الطباعة الأصلية** لنافذة Luma نفسها — تشكيلٌ عربي
  سياقي صحيح لأن الراسم هو محرك الويب الذي يرسم المحرر بعينه. الورق
  أبيض دائمًا بصرف النظر عن ثيم الشاشة، وعناصر الواجهة لا تُطبع مع
  النص، وهوامش الصفحة مضبوطة.

### المظهر والراحة

- خمسة ثيمات: ورق · ضباب · مريمية · خزامى · ليل.
- المحرر المريح بثلاث طبقات مستقلة: الآلة الكاتبة، والتركيز، وZen.
- خط الكتابة وحجمه وتباعد أسطره وعرض منطقته — كلها للمستخدم.
- خطوط النظام والمستوردة، وتغطيتها العربية مقروءة من جدول محارف الخط
  لا مخمَّنة من اسمه. Cairo وAlmarai مدمجان، **بلا شبكة**.

### الوصول والأداء

- كل عنصر تفاعلي: تسمية عربية، وحلقة تركيز ظاهرة، ومسار بلوحة المفاتيح،
  وهدف نقر ٣٢×٣٢ فعليًا لا مرسومًا فقط.
- حالة الحفظ تُعلَن لقارئ الشاشة بأولوية منخفضة بلا سحب التركيز من النص.
- شاشة تغطّي النافذة (الإعدادات، ورقة الخط) تُخرج ما تحتها من مسار
  التركيز وشجرة الوصول فعليًا — لا واجهة تعمل خلف حجاب معتم.
- تقليل الحركة من تفضيل النظام أو من تفضيل Luma — أيّهما كان.
- تسع ميزانيات أداء **مقيسة داخل التطبيق ومبوَّبة**، منها الفتح حتى
  مؤشر قابل للكتابة، وزمن ظهور الحرف، ونمو الذاكرة في جلسة ممتدة.

### الخصوصية

- كل نص على جهازك، في `~/Library/Application Support/Luma/`.
- **لا حساب، ولا مزامنة، ولا تتبّع، ولا شبكة إطلاقًا** — لا استحقاق
  شبكة في الحزمة، وسياسة CSP تمنع الاتصال أصلًا.
- لا تحديث تلقائي في هذا الإصدار — [ADR ٠٠١٣](docs/decisions/0013-auto-update.md).

### ما ليس فيه بعد

مساحة الأفكار · الإملاء الصوتي · الأصوات المحيطة · البحث داخل النص.
بنيتها موجودة في التصميم، ونقاط ارتباطها محجوزة، وبناؤها لا يغيّر ما سبق.

### قيود معروفة

- **الاجتياز بـ`Tab`** يحكمه إعداد macOS «التنقل بلوحة المفاتيح»، وهو
  مطفأ افتراضيًا: عندها لا يصل `Tab` إلا الحقول النصية — في Luma وفي كل
  تطبيق macOS أصلي.
- أدنى إصدار مدعوم **macOS 13.0**، ولم يُجرَّب عليه بعد —
  [ADR ٠٠١٢](docs/decisions/0012-minimum-macos.md).
- حدود مقيسة: مستند حتى **٢٠ ألف كلمة**، ومكتبة حتى **٥٠٠ مستند** —
  [ADR ٠٠١١](docs/decisions/0011-performance-budgets.md).
